# TMS.md

## Overview
- TM Code — IDE desktop chat-first (o agente É a interface primária) empacotada com Tauri 2. Frontend em `src/`, shell Rust em `src-tauri/`, workers Cloudflare in-repo em `workers/`.
- **Mapa canónico do sistema: `ARCHITECTURE.md` na raiz** — 4 componentes (IDE, Data-Plane, Control-Plane, Web) e quem é dono de quê. Lê-o ANTES de raciocinar sobre modelos, billing, streaming ou paralelismo. Nota histórica que contradiga uma secção "Current"/autoritativa: a "Current" vence.
- Repos relacionados: control-plane `~/dev/deskotp/toquemedia-studio-api` (**armadilha: a working tree NÃO commitada É produção — deploy sempre da tree completa, nunca de HEAD**); web + regras Firebase do sistema `~/dev/web/toquemedia-studio`; referência de agente cli-vaz `~/dev/cli-vaz` (fonte reconstruída do Claude Code; `~/dev/claude-vaz` NÃO existe).

## Stack
- React 19 + TypeScript 5.9 + Vite 8 + Zustand 5 + Chakra UI v3; Monaco Editor; xterm.js. Desktop: Tauri 2 (Rust 2021).
- Workers: `workers/ai-pass-through` (data-plane IA) e `workers/collab-signaling`.
- Testes: Jest 30 (ts-jest, jsdom) na app; o worker AI usa `node:test` via tsx (**tsx NÃO type-checka**).
- Yarn 1.22; Node >=20.19. Versão actual: 1.0.2 (`package.json` + `src-tauri/Cargo.toml`).

## Commands
- `yarn dev` (só frontend) · `yarn tauri dev` (app completa) · `yarn tauri:dev:all` (app + os 2 workers)
- `yarn test` · ficheiro único: `yarn test src/.../x.test.ts` · por nome: `yarn test -t "padrão"`
- Worker AI: `yarn test:ai-worker` + `yarn --cwd workers/ai-pass-through typecheck`
- `yarn build` = tsc + vite — **é O gate de correcção TS (não há lint)**. Nunca com pipe sem `set -o pipefail` — o pipe mascara um exit≠0.
- Release: `yarn tauri:build:mac-arm64` / `:win-x64`. **Releases cortam-se por TAG → CI publica. Nunca criar/push uma tag sem pedido explícito do developer.**

## Structure
- `src/components` (UI por área) · `src/hooks` · `src/services` (agent/, mcp, deploy, tauriFetch…) · `src/stores` (Zustand, um por domínio; services mutam via `useXStore.getState()`) · `src/theme`+`src/themes` · `src/types` · `src/utils`
- `src-tauri/src/commands/` — um módulo por domínio, registados em `lib.rs`. **Todo `#[tauri::command]` novo TEM de entrar também em `src-tauri/permissions/autogenerated.toml`** — sem entrada, o invoke é bloqueado antes do Rust ("X not allowed" não significa binário velho).
- Testes colocados em `__tests__/` junto ao código. Ficheiro de teste cujos únicos imports são `require()`/`typeof import` precisa de `export {}` top-level (senão o tsc global trata-o como script e colidem declarações).

## EntryPoints
- App: `src/main.tsx`, `src/App.tsx`; `MainLayout.tsx` alterna Welcome/Chat/Preview/Editor/Settings (`layoutStore.viewMode`).
- Agente: `src/services/agent/agentService.ts` → `queryEngine.ts`/`query.ts` (loop streaming, steering, auto-compact) → `toolExecutor.ts` (gates + execução). Prompt: `contextBuilder.ts` + `contextBuilder/sections/*` + `auxiliaryRegistry.ts`. Slash: `slashCommandRegistry.ts` + `commands/`. Compactação: `compact/`. Memória: `memdir.ts` + `memory*.ts`. TMS: `tmsBootstrap.ts`/`tmsContext.ts`.
- Data-plane: `workers/ai-pass-through/src/index.ts` — rota única `POST /v1/chat/completions`; resto = 404 `tm_not_found`.
- Rust: `src-tauri/src/lib.rs`.

## Project Patterns
### Dois backends, dois transportes (a regra nº 1)
- **Control-plane** `api-agents.toquemedia.net` (repo separado): auth, App Check, deploys, catálogo ADMIN de modelos, billing READ + ciclo (`/v1/me` faz lazy-write do tokenBudget no expiry). Chamado via `tauriFetch` (proxy Rust/reqwest) — CORS-free, não-streaming.
- **Data-plane** `ai-pass-through-worker…workers.dev` (in-repo): routing de modelos em runtime (config KV `active`), streaming SSE, e **contabilidade de billing — fonte única de verdade** (`billing.ts`). Chamado via OpenAI SDK + fetch nativo do browser — sujeito a CORS. Emite `X-TM-Model`/`X-Model-Context-Window`.
- Consequências: `curl` não prova o caminho do browser; login/billing podem funcionar com a IA silenciosamente partida (e vice-versa); **nunca reintroduzir contagem de tokens no cliente**.
### Modelos são DADOS, não código
- Adicionar/remover modelo gerido = editar a KV do data-plane, não código. A IDE manda um placeholder e lê `X-TM-Model`; `modelProfiles.ts` é apenas a tabela de fallback de capacidades. Secrets de provider vivem no DATA-plane.
### Env & URLs
- `import.meta.env` só via `src/utils/viteEnv.ts` (Jest não parseia `import.meta`; tests usam o mock em `src/utils/__mocks__/viteEnv.ts`). `devUrls.ts` ignora localhost/192.168.64.1 leaked em builds de produção. O `.env` local é gitignored e tem chaves duplicadas (last-wins — overrides no fundo).
### Convenções
- i18n: strings de UI via `useTranslation()`/`t('key')` (`src/i18n/translations.ts`); copy bilingue; comentários e commits em português.
- Theme: usar `tokens` de `src/theme/tokens.ts`, nunca valores hard-coded.
- O código carrega comentários longos de "porquê" (race conditions, bugs passados, medições de sessões) — preservar e imitar esse estilo; muitos guards existem por razões não-óbvias explicadas inline.

## Agent Rules
- Antes de editar um ficheiro existente, inspecciona o ficheiro/range actual primeiro; navegação por search/símbolos antes de leituras largas quando o alvo é desconhecido.
- Testes focados para mudanças de agente/runtime; `yarn build` para mudanças TS largas; mudanças no worker AI → `yarn test:ai-worker` + typecheck do worker.
- Mudanças à CAMADA DO AGENTE (services/agent, host/, runner/, prompt): `yarn evals:agent` verde antes do merge — é o gate local que substitui CI hospedado (custo real de tokens; ver evals/README.md).
- Preserva as alterações do developer no worktree; não reverter ficheiros alheios à tarefa.
### Self-hosting — trabalhar NESTE repo a partir do próprio TM Code
- Dogfood SÓ a partir da **build instalada**. Editar `src/` não afecta a instância instalada que te hospeda — mas numa instância `yarn tauri dev` afectaria: o HMR recarrega o webview onde o TEU loop corre. A instância dev é **alvo de verificação** (processo separado, lançado em background/PTY e observado pelos logs), nunca o hospedeiro do agente.
- **Nunca** matar a porta/árvore de processos da própria IDE — o teu processo hospedeiro morre contigo dentro.
- O `.env` deste repo está mecanicamente selado (como em todos): debugging da classe env-leak passa pelo developer, não por tentativas de leitura.
- Builds longas (`yarn tauri:build:*`, suite Jest completa, cargo) excedem o tecto de 600s do shell bloqueante: usa `execute_command_background` com `timeout_secs` explícito (tecto 3600 — o sistema acorda-te no fim, nunca sondar) ou a shell PTY persistente. Nunca o `execute_command` bloqueante.
- Abrir o mesmo projecto na instância dev dispara o aviso de double-open do window-lock — é esperado; confirma com o developer.
- Paralelismo: a política autoritativa é **F3 = um agente por projecto** (`src/services/agent/parallelTasks/policy.ts` + ARCHITECTURE.md → "Current parallel model"). Não contornar sem decisão escrita do developer nesse documento.

## Confirmed
- Billing dividido: metering por pedido é EXCLUSIVO do data-plane (usage real + commit atómico); o ciclo (reset/carry-over/anchoring) é do control-plane no read path de `/v1/me` (event-driven: launch, focus, pós-compra — nunca polled). A IDE só exibe.
- BYOK: `byokRouting.ts` é o hub; snapshot congelado POR SESSÃO; tarefas/sub-agents pinam o snapshot do run; Anthropic é 1ª classe (Messages API via `anthropicAdapter.ts`, com prompt caching); billing isenta BYOK; contas auxiliares só passam por BYOK em planos free (`resolveAuxByokRoute`).
- Prompt: o TMS.md é injectado INTEIRO no bloco estático (prompt-cache do provider) — não re-pedir via `request_context`. Doutrina full-delivery (2026-08-03): secções bounded entregues inline; on-demand só as unbounded (`project.structure_full`, `project.docs_full`, `project.symbol_index`) e `agent_runtime.memory_context`. Defs de tools MCP são DIFERIDOS — carregar via `ToolSearch`; os defs locais ficam congelados o run inteiro.
- `read_file` desenhado contra desperdício: stub para ranges não alterados, guardas de tamanho (>256KB exige offset/limit), extensões binárias rejeitadas, output com números de linha; o loop tem guard read-before-write.
- `/init` espera a estrutura compacta deste ficheiro; um TMS válido não é recriado a cada mensagem; TMS em falta é resolvido por bootstrap explícito (/init), não por lembretes.
- Team BYOK: contabilidade usa o pool partilhado da equipa, não fatias por membro.
- Tectos de execução actuais: `execute_command` 120s default/600s máx (installs 300s); `execute_command_background` 300s default/**3600s máx** (F1-8), 6 concorrentes, auto-wake; resultados de shell truncados a 8k chars (excesso via `read_large_result`).

## Inferred
- A instância dev lançada como alvo de verificação abre o último projecto/Welcome; o comportamento exacto do arranque em dogfood fica por confirmar na prática.

## Pending Confirmation
- Sucessor da política F3 para executores TM Work (decisão do developer, a registar no ARCHITECTURE.md "Current parallel model" antes de qualquer fan-out multi-agente).

## lastGeneratedAt
2026-08-03

## sourceFilesUsed
- `CLAUDE.md`
- `ARCHITECTURE.md`
- `package.json`
- `src-tauri/Cargo.toml`
- `src/services/agent/tmsBootstrap.ts`
- `src/services/agent/contextBuilder/sections/chatSections.ts`
- `src/services/agent/toolExecutor.ts`
- `src/services/agent/byokRouting.ts`
