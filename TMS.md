# TMS — Project Memory

> Auto-generated by TM Code. Last updated: 2026-05-17
> This file is read by the AI agent at the start of each session.

---

## Project Analysis

### Overview
- **Name:** toquemedia-studio (TM Code)
- **Framework:** React 19 + Tauri 2 (Rust backend)
- **Language:** TypeScript (frontend), Rust (backend)
- **Package Manager:** yarn@1.22.22
- **Monorepo:** no

### Commands
- **Install:** yarn install
- **Dev:** npm run tauri dev (full app) / npm run dev (frontend only, port 1420)
- **Build:** npm run tauri build (full production) / npm run build (tsc + vite build)
- **Test:** npm test (Jest with ts-jest)
- **Lint:** not configured

### Structure
- **src/components/** — React UI organized by domain (chat, editor, views, dialogs, welcome, debugger, http-client, prompt, ui/)
- **src/stores/** — Zustand state management (30+ stores covering core, chat/agent, layout, and feature concerns)
- **src/services/** — Tauri command wrappers and business logic (file, editor, runtime, agent orchestration, MCP, auth)
- **src-tauri/** — Rust backend: Tauri commands, state management, IPC handlers, OS integrations
- **docs/** — Documentation, benchmarking, architecture plans, UI design specs
- **scripts/** — Build and utility scripts (icon generation, skills verification)
- **tests/** — Jest unit tests and test templates

### Conventions Detected
- TypeScript strict mode enabled with `noUnusedLocals` and `noUnusedParameters`
- Path alias: `@/*` maps to `src/*`
- Tests use Jest with `ts-jest`, `jsdom` environment, and `identity-obj-proxy` for CSS mocks
- Test files follow `__tests__/*.test.{ts,tsx}` pattern
- Vite config with `vite-tsconfig-paths` for path resolution
- Monaco Editor workers bundled separately via `manualChunks`
- Cloudflare Workers integration for API proxying and deployment

### Dependencies (Key)
- **@tauri-apps/api** (v2.10.1) — Tauri IPC bridge between frontend and Rust backend
- **@chakra-ui/react** (v3.34.0) — UI component library
- **@monaco-editor/react** (v4.7.0) — VS Code-based code editor
- **zustand** (v5.0.11) — Lightweight state management
- **@xterm/xterm** (v6.0.0) — Terminal emulator
- **firebase** (v12.10.0) — Authentication and backend services
- **react-markdown** (v10.1.0) — Markdown rendering in chat
- **framer-motion** (v12.36.0) — Animations and transitions
- **prettier** (v3.8.1) — Code formatting
- **diff** (v8.0.3) — Inline diff computation for code changes

---

## Memory

### Milestones
<!-- Filled as work progresses -->

### Decisions
<!-- Filled as architectural decisions are made -->

### Pending Tasks
<!-- Filled as tasks are identified -->

---

## Custom Instructions

Add your project-specific instructions below. These will guide the AI agent.

<!-- Example:
- Always use named exports, never default exports
- Run `npm test` before committing any changes
- Keep components under 200 lines
-->
