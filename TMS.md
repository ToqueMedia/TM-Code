# TMS — Project Memory

> Auto-generated by TM Code. Last updated: 2026-06-01
> This file is read by the AI agent at the start of each session.

---

## Project Analysis

### Overview
- **Name:** toquemedia-studio (TM Code)
- **Version:** 0.7.2
- **Framework:** React 19 + Tauri 2 (Rust backend)
- **Language:** TypeScript (frontend), Rust (backend)
- **Package Manager:** yarn@1.22.22
- **Node Engine:** >=20.19.0
- **Monorepo:** no

### Related Projects
- **Backend/Proxy:** `~/dev/deskotp/toquemedia-studio-api` — API backend e proxy do TM Code
- **Web:** `~/dev/web/toquemedia-studio` — Versão web do ToqueMedia Studio
- **Claude Vaz:** `~/dev/claude-vaz` — Projeto Claude Vaz

### Commands
- **Install:** yarn install
- **Dev:** npm run tauri dev (full app) / npm run dev (frontend only, port 1420)
- **Build:** npm run tauri build (full production) / npm run build (tsc + vite build)
- **Preview:** npm run preview (build + wrangler dev)
- **Deploy:** npm run deploy (build + wrangler deploy)
- **Test:** npm test (Jest with ts-jest)
- **Test Watch:** npm run test:watch
- **Test Coverage:** npm run test:coverage
- **Platform Builds:** tauri:build:win-x64, tauri:build:mac-intel, tauri:build:mac-arm64
- **Lint:** not configured

### Structure
- **src/components/** — React UI organized by domain (auth, chat, cmd-mode, data-viewer, debugger, dialogs, editor, http-client, icons, onboarding, prompt, ui, views, welcome)
- **src/stores/** — Zustand state management (34 stores covering core, chat/agent, BYOK, billing, deploy, layout, MCP, and feature concerns)
- **src/services/** — Tauri command wrappers and business logic (agent orchestration, auth, deploy, MCP, file/editor/runtime, dev server management, LSP, notifications, updates)
- **src/hooks/** — Custom React hooks (keyboard shortcuts, editor state, file tree worker, billing, BYOK, network status, queue processing, window controls)
- **src/i18n/** — Internationalization (translations + useTranslation hook)
- **src/lib/** — Shared library code (TanStack Query client)
- **src/theme/** + **src/themes/** — Chakra UI theme tokens and ToqueMedia custom theme
- **src/types/** — Shared TypeScript type definitions (agent, chat, fileTree, project, messageQueue)
- **src/utils/** — Utility modules (crypto, logger, file tree management, Monaco bridge, platform patches, session export, signal, window management)
- **src/assets/icons/** — Static icon assets
- **src-tauri/** — Rust backend: Tauri commands, state management, IPC handlers, OS integrations (screen capture, keychain, SQLite data viewer, PTY, window vibrancy)
- **docs/** — Documentation, benchmarking, architecture plans, UI design specs
- **planning/** + **plans/** — Project planning documents and feature specs
- **scripts/** — Build and utility scripts (icon generation, build orchestration, skills verification)
- **tests/** — Jest unit tests and test templates

### Conventions Detected
- TypeScript strict mode enabled with `noUnusedLocals` and `noUnusedParameters`
- Path alias: `@/*` maps to `src/*` (resolved via Vite `resolve.tsconfigPaths: true`)
- Tests use Jest (v30) with `ts-jest`, `jsdom` environment, and `identity-obj-proxy` for CSS mocks
- Test files follow `__tests__/*.test.{ts,tsx}` pattern
- Vite 8 with `@vitejs/plugin-react`
- Monaco Editor bundled as separate chunk via `manualChunks`
- Cloudflare Workers integration via `@cloudflare/vite-plugin` and `wrangler` for deployment
- Firestore emulator proxy configured in Vite dev server (port 8082)
- Testing Library (react + user-event + jest-dom) for component tests
- `fake-indexeddb` available for IndexedDB mocking in tests

### Dependencies (Key)
- **@tauri-apps/api** (v2.11.0) — Tauri IPC bridge between frontend and Rust backend
- **@tauri-apps/plugin-*** (dialog, fs, notification, opener, process, updater) — Tauri plugin ecosystem
- **@chakra-ui/react** (v3.34.0) — UI component library
- **@monaco-editor/react** (v4.7.0) + **monaco-editor** (v0.55.1) — Code editor
- **zustand** (v5.0.11) — Lightweight state management
- **@tanstack/react-query** (v5.90.21) — Server state and data fetching
- **@tanstack/react-virtual** (v3.13.22) — Virtualized lists for large file trees
- **@xterm/xterm** (v6.0.0) — Terminal emulator with fit and web-links addons
- **firebase** (v12.10.0) — Authentication and backend services
- **react-markdown** (v10.1.0) + **remark-gfm** — Markdown rendering with GFM support
- **react-syntax-highlighter** (v16.1.1) — Code syntax highlighting in chat
- **framer-motion** (v12.36.0) — Animations and transitions
- **jose** (v6.2.3) — JWT/JWK handling
- **prettier** (v3.8.1) — Code formatting
- **diff** (v8.0.3) — Inline diff computation for code changes
- **html2canvas** (v1.4.1) — Screenshot/canvas rendering
- **react-icons** (v5.6.0) — Icon library

### Rust Backend (Key Crates)
- **tauri** (v2) — Desktop framework with macos-private-api
- **portable-pty** — Cross-platform PTY for integrated terminal
- **rusqlite** (bundled) — SQLite for in-IDE Data Viewer
- **keyring** — OS keychain access for BYOK API keys
- **screenshots** — Screen region capture for Preview screenshot feature
- **window-vibrancy** — macOS/Windows visual effects (Mica, Acrylic, vibrancy)
- **reqwest** — HTTP client for external API calls
- **tokio** — Async runtime
- **tauri-plugin-fs** (with watch) — Filesystem watching
- **tauri-plugin-localhost** — Localhost server plugin

---

## Memory

### Milestones
- 2026-06-09: `/speed` gated to pro/max plans. Service-layer check in `executeSpeed` (reads `useBillingStore.getState().plan`, whitelist `['pro','max']`) shows `speed.planRequired`. New opt-in `usesOwnPlanGate` flag in `SlashCommand` lets a paid command bypass the generic `requiresPaidPlan` guard so its own message is shown. The "Pro" badge in the slash menu still renders. Generic guard in `executePrompt`/`usePromptBar` checks `requiresPaidPlan && !usesOwnPlanGate`.
- 2026-06-02: Migrated Gemini 3.5 Flash → Gemini 3.1 Pro (thinkingBudget, 200K ctx cap, temperature 0.7/topP 0.95/topK 64)
- 2026-06-02: MiMo provider changed from api.xiaomimimo.com to opengateway.gitlawb.com (Gutlawb gateway), context window capped at 256K

### Decisions
- 2026-06-09: Gate per-command paid features in the **service layer** (e.g. `executeSpeed`), not the UI. UI is shared between ChatView and TerminalView, but the service-layer gate covers both call sites and any future invoker (queued commands, hotkeys, MCP).
- 2026-06-09: Add `usesOwnPlanGate` as an opt-in flag (not a default). Default behaviour (generic "paid feature" text + settings redirect) preserved for commands like `/te2e` that don't have custom copy.
- 2026-06-02: Gemini 3.1 Pro uses thinkingBudget (numeric) instead of thinkingLevel (string). Budget map: low=2048, medium=8192, high=24576. Always-on thinking for coding tasks (high=24576).
- 2026-06-02: Gemini 3.1 Pro context window officially 1M but capped at 200K for cost/latency balance.
- 2026-06-02: MiMo authStyle changed from 'api-key' to default Bearer (Gutlawb gateway uses standard Authorization header).

### Pending Tasks
<!-- Filled as tasks are identified -->

---

## Custom Instructions

Add your project-specific instructions below. These will guide the AI agent.

<!-- Example:
- Always use named exports, never default exports
- Run `npm test` before committing any changes
- Keep components under 200 lines
-->
