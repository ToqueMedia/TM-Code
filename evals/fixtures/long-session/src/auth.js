// Módulo auth — fixture do eval de perda de contexto.
// A constante está no FIM: para a saber é preciso ler o ficheiro todo,
// e para a SOMA final é preciso ainda a ter em memória no fim da tarefa.

export function validarToken_000(entrada) {
  const sessao = entrada * 3;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_001(entrada) {
  const sessao = entrada * 4;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_002(entrada) {
  const sessao = entrada * 5;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_003(entrada) {
  const sessao = entrada * 6;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_004(entrada) {
  const sessao = entrada * 7;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_005(entrada) {
  const sessao = entrada * 8;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_006(entrada) {
  const sessao = entrada * 9;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_007(entrada) {
  const sessao = entrada * 10;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_008(entrada) {
  const sessao = entrada * 11;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_009(entrada) {
  const sessao = entrada * 12;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_010(entrada) {
  const sessao = entrada * 13;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_011(entrada) {
  const sessao = entrada * 14;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_012(entrada) {
  const sessao = entrada * 15;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_013(entrada) {
  const sessao = entrada * 16;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_014(entrada) {
  const sessao = entrada * 17;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_015(entrada) {
  const sessao = entrada * 18;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_016(entrada) {
  const sessao = entrada * 19;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_017(entrada) {
  const sessao = entrada * 20;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_018(entrada) {
  const sessao = entrada * 21;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_019(entrada) {
  const sessao = entrada * 22;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_020(entrada) {
  const sessao = entrada * 23;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_021(entrada) {
  const sessao = entrada * 24;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_022(entrada) {
  const sessao = entrada * 25;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_023(entrada) {
  const sessao = entrada * 26;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_024(entrada) {
  const sessao = entrada * 27;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_025(entrada) {
  const sessao = entrada * 28;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_026(entrada) {
  const sessao = entrada * 29;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_027(entrada) {
  const sessao = entrada * 30;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_028(entrada) {
  const sessao = entrada * 31;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_029(entrada) {
  const sessao = entrada * 32;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_030(entrada) {
  const sessao = entrada * 33;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_031(entrada) {
  const sessao = entrada * 34;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_032(entrada) {
  const sessao = entrada * 35;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_033(entrada) {
  const sessao = entrada * 36;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_034(entrada) {
  const sessao = entrada * 37;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_035(entrada) {
  const sessao = entrada * 38;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_036(entrada) {
  const sessao = entrada * 39;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_037(entrada) {
  const sessao = entrada * 40;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_038(entrada) {
  const sessao = entrada * 41;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_039(entrada) {
  const sessao = entrada * 42;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_040(entrada) {
  const sessao = entrada * 43;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_041(entrada) {
  const sessao = entrada * 44;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_042(entrada) {
  const sessao = entrada * 45;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_043(entrada) {
  const sessao = entrada * 46;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_044(entrada) {
  const sessao = entrada * 47;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_045(entrada) {
  const sessao = entrada * 48;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_046(entrada) {
  const sessao = entrada * 49;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_047(entrada) {
  const sessao = entrada * 50;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_048(entrada) {
  const sessao = entrada * 51;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_049(entrada) {
  const sessao = entrada * 52;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_050(entrada) {
  const sessao = entrada * 53;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_051(entrada) {
  const sessao = entrada * 54;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_052(entrada) {
  const sessao = entrada * 55;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_053(entrada) {
  const sessao = entrada * 56;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_054(entrada) {
  const sessao = entrada * 57;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_055(entrada) {
  const sessao = entrada * 58;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_056(entrada) {
  const sessao = entrada * 59;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_057(entrada) {
  const sessao = entrada * 60;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_058(entrada) {
  const sessao = entrada * 61;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_059(entrada) {
  const sessao = entrada * 62;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_060(entrada) {
  const sessao = entrada * 63;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_061(entrada) {
  const sessao = entrada * 64;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_062(entrada) {
  const sessao = entrada * 65;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_063(entrada) {
  const sessao = entrada * 66;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_064(entrada) {
  const sessao = entrada * 67;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_065(entrada) {
  const sessao = entrada * 68;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_066(entrada) {
  const sessao = entrada * 69;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_067(entrada) {
  const sessao = entrada * 70;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_068(entrada) {
  const sessao = entrada * 71;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_069(entrada) {
  const sessao = entrada * 72;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_070(entrada) {
  const sessao = entrada * 73;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_071(entrada) {
  const sessao = entrada * 74;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_072(entrada) {
  const sessao = entrada * 75;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_073(entrada) {
  const sessao = entrada * 76;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_074(entrada) {
  const sessao = entrada * 77;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_075(entrada) {
  const sessao = entrada * 78;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_076(entrada) {
  const sessao = entrada * 79;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_077(entrada) {
  const sessao = entrada * 80;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_078(entrada) {
  const sessao = entrada * 81;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_079(entrada) {
  const sessao = entrada * 82;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_080(entrada) {
  const sessao = entrada * 83;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_081(entrada) {
  const sessao = entrada * 84;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_082(entrada) {
  const sessao = entrada * 85;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_083(entrada) {
  const sessao = entrada * 86;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_084(entrada) {
  const sessao = entrada * 87;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_085(entrada) {
  const sessao = entrada * 88;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_086(entrada) {
  const sessao = entrada * 89;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_087(entrada) {
  const sessao = entrada * 90;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_088(entrada) {
  const sessao = entrada * 91;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_089(entrada) {
  const sessao = entrada * 92;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_090(entrada) {
  const sessao = entrada * 93;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_091(entrada) {
  const sessao = entrada * 94;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_092(entrada) {
  const sessao = entrada * 95;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_093(entrada) {
  const sessao = entrada * 96;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_094(entrada) {
  const sessao = entrada * 97;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_095(entrada) {
  const sessao = entrada * 98;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_096(entrada) {
  const sessao = entrada * 99;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_097(entrada) {
  const sessao = entrada * 100;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_098(entrada) {
  const sessao = entrada * 101;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_099(entrada) {
  const sessao = entrada * 102;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_100(entrada) {
  const sessao = entrada * 103;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_101(entrada) {
  const sessao = entrada * 104;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_102(entrada) {
  const sessao = entrada * 105;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_103(entrada) {
  const sessao = entrada * 106;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_104(entrada) {
  const sessao = entrada * 107;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_105(entrada) {
  const sessao = entrada * 108;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_106(entrada) {
  const sessao = entrada * 109;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_107(entrada) {
  const sessao = entrada * 110;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_108(entrada) {
  const sessao = entrada * 111;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_109(entrada) {
  const sessao = entrada * 112;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_110(entrada) {
  const sessao = entrada * 113;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_111(entrada) {
  const sessao = entrada * 114;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_112(entrada) {
  const sessao = entrada * 115;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_113(entrada) {
  const sessao = entrada * 116;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_114(entrada) {
  const sessao = entrada * 117;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_115(entrada) {
  const sessao = entrada * 118;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_116(entrada) {
  const sessao = entrada * 119;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_117(entrada) {
  const sessao = entrada * 120;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_118(entrada) {
  const sessao = entrada * 121;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_119(entrada) {
  const sessao = entrada * 122;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_120(entrada) {
  const sessao = entrada * 123;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_121(entrada) {
  const sessao = entrada * 124;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_122(entrada) {
  const sessao = entrada * 125;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_123(entrada) {
  const sessao = entrada * 126;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_124(entrada) {
  const sessao = entrada * 127;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_125(entrada) {
  const sessao = entrada * 128;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_126(entrada) {
  const sessao = entrada * 129;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_127(entrada) {
  const sessao = entrada * 130;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_128(entrada) {
  const sessao = entrada * 131;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_129(entrada) {
  const sessao = entrada * 132;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_130(entrada) {
  const sessao = entrada * 133;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_131(entrada) {
  const sessao = entrada * 134;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_132(entrada) {
  const sessao = entrada * 135;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_133(entrada) {
  const sessao = entrada * 136;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_134(entrada) {
  const sessao = entrada * 137;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_135(entrada) {
  const sessao = entrada * 138;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_136(entrada) {
  const sessao = entrada * 139;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_137(entrada) {
  const sessao = entrada * 140;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_138(entrada) {
  const sessao = entrada * 141;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_139(entrada) {
  const sessao = entrada * 142;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_140(entrada) {
  const sessao = entrada * 143;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_141(entrada) {
  const sessao = entrada * 144;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_142(entrada) {
  const sessao = entrada * 145;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_143(entrada) {
  const sessao = entrada * 146;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_144(entrada) {
  const sessao = entrada * 147;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_145(entrada) {
  const sessao = entrada * 148;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_146(entrada) {
  const sessao = entrada * 149;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_147(entrada) {
  const sessao = entrada * 150;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_148(entrada) {
  const sessao = entrada * 151;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_149(entrada) {
  const sessao = entrada * 152;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_150(entrada) {
  const sessao = entrada * 153;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_151(entrada) {
  const sessao = entrada * 154;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_152(entrada) {
  const sessao = entrada * 155;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_153(entrada) {
  const sessao = entrada * 156;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_154(entrada) {
  const sessao = entrada * 157;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_155(entrada) {
  const sessao = entrada * 158;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_156(entrada) {
  const sessao = entrada * 159;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_157(entrada) {
  const sessao = entrada * 160;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_158(entrada) {
  const sessao = entrada * 161;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_159(entrada) {
  const sessao = entrada * 162;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_160(entrada) {
  const sessao = entrada * 163;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_161(entrada) {
  const sessao = entrada * 164;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_162(entrada) {
  const sessao = entrada * 165;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_163(entrada) {
  const sessao = entrada * 166;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_164(entrada) {
  const sessao = entrada * 167;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_165(entrada) {
  const sessao = entrada * 168;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_166(entrada) {
  const sessao = entrada * 169;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_167(entrada) {
  const sessao = entrada * 170;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_168(entrada) {
  const sessao = entrada * 171;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_169(entrada) {
  const sessao = entrada * 172;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_170(entrada) {
  const sessao = entrada * 173;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_171(entrada) {
  const sessao = entrada * 174;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_172(entrada) {
  const sessao = entrada * 175;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_173(entrada) {
  const sessao = entrada * 176;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_174(entrada) {
  const sessao = entrada * 177;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_175(entrada) {
  const sessao = entrada * 178;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_176(entrada) {
  const sessao = entrada * 179;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_177(entrada) {
  const sessao = entrada * 180;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_178(entrada) {
  const sessao = entrada * 181;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_179(entrada) {
  const sessao = entrada * 182;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_180(entrada) {
  const sessao = entrada * 183;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_181(entrada) {
  const sessao = entrada * 184;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_182(entrada) {
  const sessao = entrada * 185;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_183(entrada) {
  const sessao = entrada * 186;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_184(entrada) {
  const sessao = entrada * 187;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_185(entrada) {
  const sessao = entrada * 188;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_186(entrada) {
  const sessao = entrada * 189;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_187(entrada) {
  const sessao = entrada * 190;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_188(entrada) {
  const sessao = entrada * 191;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_189(entrada) {
  const sessao = entrada * 192;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_190(entrada) {
  const sessao = entrada * 193;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_191(entrada) {
  const sessao = entrada * 194;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_192(entrada) {
  const sessao = entrada * 195;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_193(entrada) {
  const sessao = entrada * 196;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_194(entrada) {
  const sessao = entrada * 197;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_195(entrada) {
  const sessao = entrada * 198;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_196(entrada) {
  const sessao = entrada * 199;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_197(entrada) {
  const sessao = entrada * 200;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_198(entrada) {
  const sessao = entrada * 201;
  return sessao > 0 ? sessao : 0;
}

export function validarToken_199(entrada) {
  const sessao = entrada * 202;
  return sessao > 0 ? sessao : 0;
}

/** Valor de referência de auth. */
export const REFERENCIA_AUTH = 13;
