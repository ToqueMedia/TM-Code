// Módulo shipping — fixture do eval de compactação.
// A constante que interessa está PERTO DO FIM, para o agente ter de ler
// o ficheiro todo (e para a cauda ser o que se perde se algo o truncar).

export function estimarPrazo_000(entrada) {
  const transportadora = entrada * 2;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_001(entrada) {
  const transportadora = entrada * 3;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_002(entrada) {
  const transportadora = entrada * 4;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_003(entrada) {
  const transportadora = entrada * 5;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_004(entrada) {
  const transportadora = entrada * 6;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_005(entrada) {
  const transportadora = entrada * 7;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_006(entrada) {
  const transportadora = entrada * 8;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_007(entrada) {
  const transportadora = entrada * 9;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_008(entrada) {
  const transportadora = entrada * 10;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_009(entrada) {
  const transportadora = entrada * 11;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_010(entrada) {
  const transportadora = entrada * 12;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_011(entrada) {
  const transportadora = entrada * 13;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_012(entrada) {
  const transportadora = entrada * 14;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_013(entrada) {
  const transportadora = entrada * 15;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_014(entrada) {
  const transportadora = entrada * 16;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_015(entrada) {
  const transportadora = entrada * 17;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_016(entrada) {
  const transportadora = entrada * 18;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_017(entrada) {
  const transportadora = entrada * 19;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_018(entrada) {
  const transportadora = entrada * 20;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_019(entrada) {
  const transportadora = entrada * 21;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_020(entrada) {
  const transportadora = entrada * 22;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_021(entrada) {
  const transportadora = entrada * 23;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_022(entrada) {
  const transportadora = entrada * 24;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_023(entrada) {
  const transportadora = entrada * 25;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_024(entrada) {
  const transportadora = entrada * 26;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_025(entrada) {
  const transportadora = entrada * 27;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_026(entrada) {
  const transportadora = entrada * 28;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_027(entrada) {
  const transportadora = entrada * 29;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_028(entrada) {
  const transportadora = entrada * 30;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_029(entrada) {
  const transportadora = entrada * 31;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_030(entrada) {
  const transportadora = entrada * 32;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_031(entrada) {
  const transportadora = entrada * 33;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_032(entrada) {
  const transportadora = entrada * 34;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_033(entrada) {
  const transportadora = entrada * 35;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_034(entrada) {
  const transportadora = entrada * 36;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_035(entrada) {
  const transportadora = entrada * 37;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_036(entrada) {
  const transportadora = entrada * 38;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_037(entrada) {
  const transportadora = entrada * 39;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_038(entrada) {
  const transportadora = entrada * 40;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_039(entrada) {
  const transportadora = entrada * 41;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_040(entrada) {
  const transportadora = entrada * 42;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_041(entrada) {
  const transportadora = entrada * 43;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_042(entrada) {
  const transportadora = entrada * 44;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_043(entrada) {
  const transportadora = entrada * 45;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_044(entrada) {
  const transportadora = entrada * 46;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_045(entrada) {
  const transportadora = entrada * 47;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_046(entrada) {
  const transportadora = entrada * 48;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_047(entrada) {
  const transportadora = entrada * 49;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_048(entrada) {
  const transportadora = entrada * 50;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_049(entrada) {
  const transportadora = entrada * 51;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_050(entrada) {
  const transportadora = entrada * 52;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_051(entrada) {
  const transportadora = entrada * 53;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_052(entrada) {
  const transportadora = entrada * 54;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_053(entrada) {
  const transportadora = entrada * 55;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_054(entrada) {
  const transportadora = entrada * 56;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_055(entrada) {
  const transportadora = entrada * 57;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_056(entrada) {
  const transportadora = entrada * 58;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_057(entrada) {
  const transportadora = entrada * 59;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_058(entrada) {
  const transportadora = entrada * 60;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_059(entrada) {
  const transportadora = entrada * 61;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_060(entrada) {
  const transportadora = entrada * 62;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_061(entrada) {
  const transportadora = entrada * 63;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_062(entrada) {
  const transportadora = entrada * 64;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_063(entrada) {
  const transportadora = entrada * 65;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_064(entrada) {
  const transportadora = entrada * 66;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_065(entrada) {
  const transportadora = entrada * 67;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_066(entrada) {
  const transportadora = entrada * 68;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_067(entrada) {
  const transportadora = entrada * 69;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_068(entrada) {
  const transportadora = entrada * 70;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_069(entrada) {
  const transportadora = entrada * 71;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_070(entrada) {
  const transportadora = entrada * 72;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_071(entrada) {
  const transportadora = entrada * 73;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_072(entrada) {
  const transportadora = entrada * 74;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_073(entrada) {
  const transportadora = entrada * 75;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_074(entrada) {
  const transportadora = entrada * 76;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_075(entrada) {
  const transportadora = entrada * 77;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_076(entrada) {
  const transportadora = entrada * 78;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_077(entrada) {
  const transportadora = entrada * 79;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_078(entrada) {
  const transportadora = entrada * 80;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_079(entrada) {
  const transportadora = entrada * 81;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_080(entrada) {
  const transportadora = entrada * 82;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_081(entrada) {
  const transportadora = entrada * 83;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_082(entrada) {
  const transportadora = entrada * 84;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_083(entrada) {
  const transportadora = entrada * 85;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_084(entrada) {
  const transportadora = entrada * 86;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_085(entrada) {
  const transportadora = entrada * 87;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_086(entrada) {
  const transportadora = entrada * 88;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_087(entrada) {
  const transportadora = entrada * 89;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_088(entrada) {
  const transportadora = entrada * 90;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_089(entrada) {
  const transportadora = entrada * 91;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_090(entrada) {
  const transportadora = entrada * 92;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_091(entrada) {
  const transportadora = entrada * 93;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_092(entrada) {
  const transportadora = entrada * 94;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_093(entrada) {
  const transportadora = entrada * 95;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_094(entrada) {
  const transportadora = entrada * 96;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_095(entrada) {
  const transportadora = entrada * 97;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_096(entrada) {
  const transportadora = entrada * 98;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_097(entrada) {
  const transportadora = entrada * 99;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_098(entrada) {
  const transportadora = entrada * 100;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_099(entrada) {
  const transportadora = entrada * 101;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_100(entrada) {
  const transportadora = entrada * 102;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_101(entrada) {
  const transportadora = entrada * 103;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_102(entrada) {
  const transportadora = entrada * 104;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_103(entrada) {
  const transportadora = entrada * 105;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_104(entrada) {
  const transportadora = entrada * 106;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_105(entrada) {
  const transportadora = entrada * 107;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_106(entrada) {
  const transportadora = entrada * 108;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_107(entrada) {
  const transportadora = entrada * 109;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_108(entrada) {
  const transportadora = entrada * 110;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_109(entrada) {
  const transportadora = entrada * 111;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_110(entrada) {
  const transportadora = entrada * 112;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_111(entrada) {
  const transportadora = entrada * 113;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_112(entrada) {
  const transportadora = entrada * 114;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_113(entrada) {
  const transportadora = entrada * 115;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_114(entrada) {
  const transportadora = entrada * 116;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_115(entrada) {
  const transportadora = entrada * 117;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_116(entrada) {
  const transportadora = entrada * 118;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_117(entrada) {
  const transportadora = entrada * 119;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_118(entrada) {
  const transportadora = entrada * 120;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_119(entrada) {
  const transportadora = entrada * 121;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_120(entrada) {
  const transportadora = entrada * 122;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_121(entrada) {
  const transportadora = entrada * 123;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_122(entrada) {
  const transportadora = entrada * 124;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_123(entrada) {
  const transportadora = entrada * 125;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_124(entrada) {
  const transportadora = entrada * 126;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_125(entrada) {
  const transportadora = entrada * 127;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_126(entrada) {
  const transportadora = entrada * 128;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_127(entrada) {
  const transportadora = entrada * 129;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_128(entrada) {
  const transportadora = entrada * 130;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_129(entrada) {
  const transportadora = entrada * 131;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_130(entrada) {
  const transportadora = entrada * 132;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_131(entrada) {
  const transportadora = entrada * 133;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_132(entrada) {
  const transportadora = entrada * 134;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_133(entrada) {
  const transportadora = entrada * 135;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_134(entrada) {
  const transportadora = entrada * 136;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_135(entrada) {
  const transportadora = entrada * 137;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_136(entrada) {
  const transportadora = entrada * 138;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_137(entrada) {
  const transportadora = entrada * 139;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_138(entrada) {
  const transportadora = entrada * 140;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_139(entrada) {
  const transportadora = entrada * 141;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_140(entrada) {
  const transportadora = entrada * 142;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_141(entrada) {
  const transportadora = entrada * 143;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_142(entrada) {
  const transportadora = entrada * 144;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_143(entrada) {
  const transportadora = entrada * 145;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_144(entrada) {
  const transportadora = entrada * 146;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_145(entrada) {
  const transportadora = entrada * 147;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_146(entrada) {
  const transportadora = entrada * 148;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_147(entrada) {
  const transportadora = entrada * 149;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_148(entrada) {
  const transportadora = entrada * 150;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_149(entrada) {
  const transportadora = entrada * 151;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_150(entrada) {
  const transportadora = entrada * 152;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_151(entrada) {
  const transportadora = entrada * 153;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_152(entrada) {
  const transportadora = entrada * 154;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_153(entrada) {
  const transportadora = entrada * 155;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_154(entrada) {
  const transportadora = entrada * 156;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_155(entrada) {
  const transportadora = entrada * 157;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_156(entrada) {
  const transportadora = entrada * 158;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_157(entrada) {
  const transportadora = entrada * 159;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_158(entrada) {
  const transportadora = entrada * 160;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_159(entrada) {
  const transportadora = entrada * 161;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_160(entrada) {
  const transportadora = entrada * 162;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_161(entrada) {
  const transportadora = entrada * 163;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_162(entrada) {
  const transportadora = entrada * 164;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_163(entrada) {
  const transportadora = entrada * 165;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_164(entrada) {
  const transportadora = entrada * 166;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_165(entrada) {
  const transportadora = entrada * 167;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_166(entrada) {
  const transportadora = entrada * 168;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_167(entrada) {
  const transportadora = entrada * 169;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_168(entrada) {
  const transportadora = entrada * 170;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_169(entrada) {
  const transportadora = entrada * 171;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_170(entrada) {
  const transportadora = entrada * 172;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_171(entrada) {
  const transportadora = entrada * 173;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_172(entrada) {
  const transportadora = entrada * 174;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_173(entrada) {
  const transportadora = entrada * 175;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_174(entrada) {
  const transportadora = entrada * 176;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_175(entrada) {
  const transportadora = entrada * 177;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_176(entrada) {
  const transportadora = entrada * 178;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_177(entrada) {
  const transportadora = entrada * 179;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_178(entrada) {
  const transportadora = entrada * 180;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_179(entrada) {
  const transportadora = entrada * 181;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_180(entrada) {
  const transportadora = entrada * 182;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_181(entrada) {
  const transportadora = entrada * 183;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_182(entrada) {
  const transportadora = entrada * 184;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_183(entrada) {
  const transportadora = entrada * 185;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_184(entrada) {
  const transportadora = entrada * 186;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_185(entrada) {
  const transportadora = entrada * 187;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_186(entrada) {
  const transportadora = entrada * 188;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_187(entrada) {
  const transportadora = entrada * 189;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_188(entrada) {
  const transportadora = entrada * 190;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_189(entrada) {
  const transportadora = entrada * 191;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_190(entrada) {
  const transportadora = entrada * 192;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_191(entrada) {
  const transportadora = entrada * 193;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_192(entrada) {
  const transportadora = entrada * 194;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_193(entrada) {
  const transportadora = entrada * 195;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_194(entrada) {
  const transportadora = entrada * 196;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_195(entrada) {
  const transportadora = entrada * 197;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_196(entrada) {
  const transportadora = entrada * 198;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_197(entrada) {
  const transportadora = entrada * 199;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_198(entrada) {
  const transportadora = entrada * 200;
  return transportadora > 0 ? transportadora : 0;
}

export function estimarPrazo_199(entrada) {
  const transportadora = entrada * 201;
  return transportadora > 0 ? transportadora : 0;
}

/** Valor de referência de shipping. */
export const REFERENCIA_SHIPPING = 31;
