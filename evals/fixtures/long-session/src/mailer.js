// Módulo mailer — fixture do eval de perda de contexto.
// A constante está no FIM: para a saber é preciso ler o ficheiro todo,
// e para a SOMA final é preciso ainda a ter em memória no fim da tarefa.

export function enviar_000(entrada) {
  const destino = entrada * 3;
  return destino > 0 ? destino : 0;
}

export function enviar_001(entrada) {
  const destino = entrada * 4;
  return destino > 0 ? destino : 0;
}

export function enviar_002(entrada) {
  const destino = entrada * 5;
  return destino > 0 ? destino : 0;
}

export function enviar_003(entrada) {
  const destino = entrada * 6;
  return destino > 0 ? destino : 0;
}

export function enviar_004(entrada) {
  const destino = entrada * 7;
  return destino > 0 ? destino : 0;
}

export function enviar_005(entrada) {
  const destino = entrada * 8;
  return destino > 0 ? destino : 0;
}

export function enviar_006(entrada) {
  const destino = entrada * 9;
  return destino > 0 ? destino : 0;
}

export function enviar_007(entrada) {
  const destino = entrada * 10;
  return destino > 0 ? destino : 0;
}

export function enviar_008(entrada) {
  const destino = entrada * 11;
  return destino > 0 ? destino : 0;
}

export function enviar_009(entrada) {
  const destino = entrada * 12;
  return destino > 0 ? destino : 0;
}

export function enviar_010(entrada) {
  const destino = entrada * 13;
  return destino > 0 ? destino : 0;
}

export function enviar_011(entrada) {
  const destino = entrada * 14;
  return destino > 0 ? destino : 0;
}

export function enviar_012(entrada) {
  const destino = entrada * 15;
  return destino > 0 ? destino : 0;
}

export function enviar_013(entrada) {
  const destino = entrada * 16;
  return destino > 0 ? destino : 0;
}

export function enviar_014(entrada) {
  const destino = entrada * 17;
  return destino > 0 ? destino : 0;
}

export function enviar_015(entrada) {
  const destino = entrada * 18;
  return destino > 0 ? destino : 0;
}

export function enviar_016(entrada) {
  const destino = entrada * 19;
  return destino > 0 ? destino : 0;
}

export function enviar_017(entrada) {
  const destino = entrada * 20;
  return destino > 0 ? destino : 0;
}

export function enviar_018(entrada) {
  const destino = entrada * 21;
  return destino > 0 ? destino : 0;
}

export function enviar_019(entrada) {
  const destino = entrada * 22;
  return destino > 0 ? destino : 0;
}

export function enviar_020(entrada) {
  const destino = entrada * 23;
  return destino > 0 ? destino : 0;
}

export function enviar_021(entrada) {
  const destino = entrada * 24;
  return destino > 0 ? destino : 0;
}

export function enviar_022(entrada) {
  const destino = entrada * 25;
  return destino > 0 ? destino : 0;
}

export function enviar_023(entrada) {
  const destino = entrada * 26;
  return destino > 0 ? destino : 0;
}

export function enviar_024(entrada) {
  const destino = entrada * 27;
  return destino > 0 ? destino : 0;
}

export function enviar_025(entrada) {
  const destino = entrada * 28;
  return destino > 0 ? destino : 0;
}

export function enviar_026(entrada) {
  const destino = entrada * 29;
  return destino > 0 ? destino : 0;
}

export function enviar_027(entrada) {
  const destino = entrada * 30;
  return destino > 0 ? destino : 0;
}

export function enviar_028(entrada) {
  const destino = entrada * 31;
  return destino > 0 ? destino : 0;
}

export function enviar_029(entrada) {
  const destino = entrada * 32;
  return destino > 0 ? destino : 0;
}

export function enviar_030(entrada) {
  const destino = entrada * 33;
  return destino > 0 ? destino : 0;
}

export function enviar_031(entrada) {
  const destino = entrada * 34;
  return destino > 0 ? destino : 0;
}

export function enviar_032(entrada) {
  const destino = entrada * 35;
  return destino > 0 ? destino : 0;
}

export function enviar_033(entrada) {
  const destino = entrada * 36;
  return destino > 0 ? destino : 0;
}

export function enviar_034(entrada) {
  const destino = entrada * 37;
  return destino > 0 ? destino : 0;
}

export function enviar_035(entrada) {
  const destino = entrada * 38;
  return destino > 0 ? destino : 0;
}

export function enviar_036(entrada) {
  const destino = entrada * 39;
  return destino > 0 ? destino : 0;
}

export function enviar_037(entrada) {
  const destino = entrada * 40;
  return destino > 0 ? destino : 0;
}

export function enviar_038(entrada) {
  const destino = entrada * 41;
  return destino > 0 ? destino : 0;
}

export function enviar_039(entrada) {
  const destino = entrada * 42;
  return destino > 0 ? destino : 0;
}

export function enviar_040(entrada) {
  const destino = entrada * 43;
  return destino > 0 ? destino : 0;
}

export function enviar_041(entrada) {
  const destino = entrada * 44;
  return destino > 0 ? destino : 0;
}

export function enviar_042(entrada) {
  const destino = entrada * 45;
  return destino > 0 ? destino : 0;
}

export function enviar_043(entrada) {
  const destino = entrada * 46;
  return destino > 0 ? destino : 0;
}

export function enviar_044(entrada) {
  const destino = entrada * 47;
  return destino > 0 ? destino : 0;
}

export function enviar_045(entrada) {
  const destino = entrada * 48;
  return destino > 0 ? destino : 0;
}

export function enviar_046(entrada) {
  const destino = entrada * 49;
  return destino > 0 ? destino : 0;
}

export function enviar_047(entrada) {
  const destino = entrada * 50;
  return destino > 0 ? destino : 0;
}

export function enviar_048(entrada) {
  const destino = entrada * 51;
  return destino > 0 ? destino : 0;
}

export function enviar_049(entrada) {
  const destino = entrada * 52;
  return destino > 0 ? destino : 0;
}

export function enviar_050(entrada) {
  const destino = entrada * 53;
  return destino > 0 ? destino : 0;
}

export function enviar_051(entrada) {
  const destino = entrada * 54;
  return destino > 0 ? destino : 0;
}

export function enviar_052(entrada) {
  const destino = entrada * 55;
  return destino > 0 ? destino : 0;
}

export function enviar_053(entrada) {
  const destino = entrada * 56;
  return destino > 0 ? destino : 0;
}

export function enviar_054(entrada) {
  const destino = entrada * 57;
  return destino > 0 ? destino : 0;
}

export function enviar_055(entrada) {
  const destino = entrada * 58;
  return destino > 0 ? destino : 0;
}

export function enviar_056(entrada) {
  const destino = entrada * 59;
  return destino > 0 ? destino : 0;
}

export function enviar_057(entrada) {
  const destino = entrada * 60;
  return destino > 0 ? destino : 0;
}

export function enviar_058(entrada) {
  const destino = entrada * 61;
  return destino > 0 ? destino : 0;
}

export function enviar_059(entrada) {
  const destino = entrada * 62;
  return destino > 0 ? destino : 0;
}

export function enviar_060(entrada) {
  const destino = entrada * 63;
  return destino > 0 ? destino : 0;
}

export function enviar_061(entrada) {
  const destino = entrada * 64;
  return destino > 0 ? destino : 0;
}

export function enviar_062(entrada) {
  const destino = entrada * 65;
  return destino > 0 ? destino : 0;
}

export function enviar_063(entrada) {
  const destino = entrada * 66;
  return destino > 0 ? destino : 0;
}

export function enviar_064(entrada) {
  const destino = entrada * 67;
  return destino > 0 ? destino : 0;
}

export function enviar_065(entrada) {
  const destino = entrada * 68;
  return destino > 0 ? destino : 0;
}

export function enviar_066(entrada) {
  const destino = entrada * 69;
  return destino > 0 ? destino : 0;
}

export function enviar_067(entrada) {
  const destino = entrada * 70;
  return destino > 0 ? destino : 0;
}

export function enviar_068(entrada) {
  const destino = entrada * 71;
  return destino > 0 ? destino : 0;
}

export function enviar_069(entrada) {
  const destino = entrada * 72;
  return destino > 0 ? destino : 0;
}

export function enviar_070(entrada) {
  const destino = entrada * 73;
  return destino > 0 ? destino : 0;
}

export function enviar_071(entrada) {
  const destino = entrada * 74;
  return destino > 0 ? destino : 0;
}

export function enviar_072(entrada) {
  const destino = entrada * 75;
  return destino > 0 ? destino : 0;
}

export function enviar_073(entrada) {
  const destino = entrada * 76;
  return destino > 0 ? destino : 0;
}

export function enviar_074(entrada) {
  const destino = entrada * 77;
  return destino > 0 ? destino : 0;
}

export function enviar_075(entrada) {
  const destino = entrada * 78;
  return destino > 0 ? destino : 0;
}

export function enviar_076(entrada) {
  const destino = entrada * 79;
  return destino > 0 ? destino : 0;
}

export function enviar_077(entrada) {
  const destino = entrada * 80;
  return destino > 0 ? destino : 0;
}

export function enviar_078(entrada) {
  const destino = entrada * 81;
  return destino > 0 ? destino : 0;
}

export function enviar_079(entrada) {
  const destino = entrada * 82;
  return destino > 0 ? destino : 0;
}

export function enviar_080(entrada) {
  const destino = entrada * 83;
  return destino > 0 ? destino : 0;
}

export function enviar_081(entrada) {
  const destino = entrada * 84;
  return destino > 0 ? destino : 0;
}

export function enviar_082(entrada) {
  const destino = entrada * 85;
  return destino > 0 ? destino : 0;
}

export function enviar_083(entrada) {
  const destino = entrada * 86;
  return destino > 0 ? destino : 0;
}

export function enviar_084(entrada) {
  const destino = entrada * 87;
  return destino > 0 ? destino : 0;
}

export function enviar_085(entrada) {
  const destino = entrada * 88;
  return destino > 0 ? destino : 0;
}

export function enviar_086(entrada) {
  const destino = entrada * 89;
  return destino > 0 ? destino : 0;
}

export function enviar_087(entrada) {
  const destino = entrada * 90;
  return destino > 0 ? destino : 0;
}

export function enviar_088(entrada) {
  const destino = entrada * 91;
  return destino > 0 ? destino : 0;
}

export function enviar_089(entrada) {
  const destino = entrada * 92;
  return destino > 0 ? destino : 0;
}

export function enviar_090(entrada) {
  const destino = entrada * 93;
  return destino > 0 ? destino : 0;
}

export function enviar_091(entrada) {
  const destino = entrada * 94;
  return destino > 0 ? destino : 0;
}

export function enviar_092(entrada) {
  const destino = entrada * 95;
  return destino > 0 ? destino : 0;
}

export function enviar_093(entrada) {
  const destino = entrada * 96;
  return destino > 0 ? destino : 0;
}

export function enviar_094(entrada) {
  const destino = entrada * 97;
  return destino > 0 ? destino : 0;
}

export function enviar_095(entrada) {
  const destino = entrada * 98;
  return destino > 0 ? destino : 0;
}

export function enviar_096(entrada) {
  const destino = entrada * 99;
  return destino > 0 ? destino : 0;
}

export function enviar_097(entrada) {
  const destino = entrada * 100;
  return destino > 0 ? destino : 0;
}

export function enviar_098(entrada) {
  const destino = entrada * 101;
  return destino > 0 ? destino : 0;
}

export function enviar_099(entrada) {
  const destino = entrada * 102;
  return destino > 0 ? destino : 0;
}

export function enviar_100(entrada) {
  const destino = entrada * 103;
  return destino > 0 ? destino : 0;
}

export function enviar_101(entrada) {
  const destino = entrada * 104;
  return destino > 0 ? destino : 0;
}

export function enviar_102(entrada) {
  const destino = entrada * 105;
  return destino > 0 ? destino : 0;
}

export function enviar_103(entrada) {
  const destino = entrada * 106;
  return destino > 0 ? destino : 0;
}

export function enviar_104(entrada) {
  const destino = entrada * 107;
  return destino > 0 ? destino : 0;
}

export function enviar_105(entrada) {
  const destino = entrada * 108;
  return destino > 0 ? destino : 0;
}

export function enviar_106(entrada) {
  const destino = entrada * 109;
  return destino > 0 ? destino : 0;
}

export function enviar_107(entrada) {
  const destino = entrada * 110;
  return destino > 0 ? destino : 0;
}

export function enviar_108(entrada) {
  const destino = entrada * 111;
  return destino > 0 ? destino : 0;
}

export function enviar_109(entrada) {
  const destino = entrada * 112;
  return destino > 0 ? destino : 0;
}

export function enviar_110(entrada) {
  const destino = entrada * 113;
  return destino > 0 ? destino : 0;
}

export function enviar_111(entrada) {
  const destino = entrada * 114;
  return destino > 0 ? destino : 0;
}

export function enviar_112(entrada) {
  const destino = entrada * 115;
  return destino > 0 ? destino : 0;
}

export function enviar_113(entrada) {
  const destino = entrada * 116;
  return destino > 0 ? destino : 0;
}

export function enviar_114(entrada) {
  const destino = entrada * 117;
  return destino > 0 ? destino : 0;
}

export function enviar_115(entrada) {
  const destino = entrada * 118;
  return destino > 0 ? destino : 0;
}

export function enviar_116(entrada) {
  const destino = entrada * 119;
  return destino > 0 ? destino : 0;
}

export function enviar_117(entrada) {
  const destino = entrada * 120;
  return destino > 0 ? destino : 0;
}

export function enviar_118(entrada) {
  const destino = entrada * 121;
  return destino > 0 ? destino : 0;
}

export function enviar_119(entrada) {
  const destino = entrada * 122;
  return destino > 0 ? destino : 0;
}

export function enviar_120(entrada) {
  const destino = entrada * 123;
  return destino > 0 ? destino : 0;
}

export function enviar_121(entrada) {
  const destino = entrada * 124;
  return destino > 0 ? destino : 0;
}

export function enviar_122(entrada) {
  const destino = entrada * 125;
  return destino > 0 ? destino : 0;
}

export function enviar_123(entrada) {
  const destino = entrada * 126;
  return destino > 0 ? destino : 0;
}

export function enviar_124(entrada) {
  const destino = entrada * 127;
  return destino > 0 ? destino : 0;
}

export function enviar_125(entrada) {
  const destino = entrada * 128;
  return destino > 0 ? destino : 0;
}

export function enviar_126(entrada) {
  const destino = entrada * 129;
  return destino > 0 ? destino : 0;
}

export function enviar_127(entrada) {
  const destino = entrada * 130;
  return destino > 0 ? destino : 0;
}

export function enviar_128(entrada) {
  const destino = entrada * 131;
  return destino > 0 ? destino : 0;
}

export function enviar_129(entrada) {
  const destino = entrada * 132;
  return destino > 0 ? destino : 0;
}

export function enviar_130(entrada) {
  const destino = entrada * 133;
  return destino > 0 ? destino : 0;
}

export function enviar_131(entrada) {
  const destino = entrada * 134;
  return destino > 0 ? destino : 0;
}

export function enviar_132(entrada) {
  const destino = entrada * 135;
  return destino > 0 ? destino : 0;
}

export function enviar_133(entrada) {
  const destino = entrada * 136;
  return destino > 0 ? destino : 0;
}

export function enviar_134(entrada) {
  const destino = entrada * 137;
  return destino > 0 ? destino : 0;
}

export function enviar_135(entrada) {
  const destino = entrada * 138;
  return destino > 0 ? destino : 0;
}

export function enviar_136(entrada) {
  const destino = entrada * 139;
  return destino > 0 ? destino : 0;
}

export function enviar_137(entrada) {
  const destino = entrada * 140;
  return destino > 0 ? destino : 0;
}

export function enviar_138(entrada) {
  const destino = entrada * 141;
  return destino > 0 ? destino : 0;
}

export function enviar_139(entrada) {
  const destino = entrada * 142;
  return destino > 0 ? destino : 0;
}

export function enviar_140(entrada) {
  const destino = entrada * 143;
  return destino > 0 ? destino : 0;
}

export function enviar_141(entrada) {
  const destino = entrada * 144;
  return destino > 0 ? destino : 0;
}

export function enviar_142(entrada) {
  const destino = entrada * 145;
  return destino > 0 ? destino : 0;
}

export function enviar_143(entrada) {
  const destino = entrada * 146;
  return destino > 0 ? destino : 0;
}

export function enviar_144(entrada) {
  const destino = entrada * 147;
  return destino > 0 ? destino : 0;
}

export function enviar_145(entrada) {
  const destino = entrada * 148;
  return destino > 0 ? destino : 0;
}

export function enviar_146(entrada) {
  const destino = entrada * 149;
  return destino > 0 ? destino : 0;
}

export function enviar_147(entrada) {
  const destino = entrada * 150;
  return destino > 0 ? destino : 0;
}

export function enviar_148(entrada) {
  const destino = entrada * 151;
  return destino > 0 ? destino : 0;
}

export function enviar_149(entrada) {
  const destino = entrada * 152;
  return destino > 0 ? destino : 0;
}

export function enviar_150(entrada) {
  const destino = entrada * 153;
  return destino > 0 ? destino : 0;
}

export function enviar_151(entrada) {
  const destino = entrada * 154;
  return destino > 0 ? destino : 0;
}

export function enviar_152(entrada) {
  const destino = entrada * 155;
  return destino > 0 ? destino : 0;
}

export function enviar_153(entrada) {
  const destino = entrada * 156;
  return destino > 0 ? destino : 0;
}

export function enviar_154(entrada) {
  const destino = entrada * 157;
  return destino > 0 ? destino : 0;
}

export function enviar_155(entrada) {
  const destino = entrada * 158;
  return destino > 0 ? destino : 0;
}

export function enviar_156(entrada) {
  const destino = entrada * 159;
  return destino > 0 ? destino : 0;
}

export function enviar_157(entrada) {
  const destino = entrada * 160;
  return destino > 0 ? destino : 0;
}

export function enviar_158(entrada) {
  const destino = entrada * 161;
  return destino > 0 ? destino : 0;
}

export function enviar_159(entrada) {
  const destino = entrada * 162;
  return destino > 0 ? destino : 0;
}

export function enviar_160(entrada) {
  const destino = entrada * 163;
  return destino > 0 ? destino : 0;
}

export function enviar_161(entrada) {
  const destino = entrada * 164;
  return destino > 0 ? destino : 0;
}

export function enviar_162(entrada) {
  const destino = entrada * 165;
  return destino > 0 ? destino : 0;
}

export function enviar_163(entrada) {
  const destino = entrada * 166;
  return destino > 0 ? destino : 0;
}

export function enviar_164(entrada) {
  const destino = entrada * 167;
  return destino > 0 ? destino : 0;
}

export function enviar_165(entrada) {
  const destino = entrada * 168;
  return destino > 0 ? destino : 0;
}

export function enviar_166(entrada) {
  const destino = entrada * 169;
  return destino > 0 ? destino : 0;
}

export function enviar_167(entrada) {
  const destino = entrada * 170;
  return destino > 0 ? destino : 0;
}

export function enviar_168(entrada) {
  const destino = entrada * 171;
  return destino > 0 ? destino : 0;
}

export function enviar_169(entrada) {
  const destino = entrada * 172;
  return destino > 0 ? destino : 0;
}

export function enviar_170(entrada) {
  const destino = entrada * 173;
  return destino > 0 ? destino : 0;
}

export function enviar_171(entrada) {
  const destino = entrada * 174;
  return destino > 0 ? destino : 0;
}

export function enviar_172(entrada) {
  const destino = entrada * 175;
  return destino > 0 ? destino : 0;
}

export function enviar_173(entrada) {
  const destino = entrada * 176;
  return destino > 0 ? destino : 0;
}

export function enviar_174(entrada) {
  const destino = entrada * 177;
  return destino > 0 ? destino : 0;
}

export function enviar_175(entrada) {
  const destino = entrada * 178;
  return destino > 0 ? destino : 0;
}

export function enviar_176(entrada) {
  const destino = entrada * 179;
  return destino > 0 ? destino : 0;
}

export function enviar_177(entrada) {
  const destino = entrada * 180;
  return destino > 0 ? destino : 0;
}

export function enviar_178(entrada) {
  const destino = entrada * 181;
  return destino > 0 ? destino : 0;
}

export function enviar_179(entrada) {
  const destino = entrada * 182;
  return destino > 0 ? destino : 0;
}

export function enviar_180(entrada) {
  const destino = entrada * 183;
  return destino > 0 ? destino : 0;
}

export function enviar_181(entrada) {
  const destino = entrada * 184;
  return destino > 0 ? destino : 0;
}

export function enviar_182(entrada) {
  const destino = entrada * 185;
  return destino > 0 ? destino : 0;
}

export function enviar_183(entrada) {
  const destino = entrada * 186;
  return destino > 0 ? destino : 0;
}

export function enviar_184(entrada) {
  const destino = entrada * 187;
  return destino > 0 ? destino : 0;
}

export function enviar_185(entrada) {
  const destino = entrada * 188;
  return destino > 0 ? destino : 0;
}

export function enviar_186(entrada) {
  const destino = entrada * 189;
  return destino > 0 ? destino : 0;
}

export function enviar_187(entrada) {
  const destino = entrada * 190;
  return destino > 0 ? destino : 0;
}

export function enviar_188(entrada) {
  const destino = entrada * 191;
  return destino > 0 ? destino : 0;
}

export function enviar_189(entrada) {
  const destino = entrada * 192;
  return destino > 0 ? destino : 0;
}

export function enviar_190(entrada) {
  const destino = entrada * 193;
  return destino > 0 ? destino : 0;
}

export function enviar_191(entrada) {
  const destino = entrada * 194;
  return destino > 0 ? destino : 0;
}

export function enviar_192(entrada) {
  const destino = entrada * 195;
  return destino > 0 ? destino : 0;
}

export function enviar_193(entrada) {
  const destino = entrada * 196;
  return destino > 0 ? destino : 0;
}

export function enviar_194(entrada) {
  const destino = entrada * 197;
  return destino > 0 ? destino : 0;
}

export function enviar_195(entrada) {
  const destino = entrada * 198;
  return destino > 0 ? destino : 0;
}

export function enviar_196(entrada) {
  const destino = entrada * 199;
  return destino > 0 ? destino : 0;
}

export function enviar_197(entrada) {
  const destino = entrada * 200;
  return destino > 0 ? destino : 0;
}

export function enviar_198(entrada) {
  const destino = entrada * 201;
  return destino > 0 ? destino : 0;
}

export function enviar_199(entrada) {
  const destino = entrada * 202;
  return destino > 0 ? destino : 0;
}

/** Valor de referência de mailer. */
export const REFERENCIA_MAILER = 7;
