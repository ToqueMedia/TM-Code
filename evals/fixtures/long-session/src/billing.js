// Módulo billing — fixture do eval de compactação.
// A constante que interessa está PERTO DO FIM, para o agente ter de ler
// o ficheiro todo (e para a cauda ser o que se perde se algo o truncar).

export function calcularFactura_000(entrada) {
  const imposto = entrada * 2;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_001(entrada) {
  const imposto = entrada * 3;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_002(entrada) {
  const imposto = entrada * 4;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_003(entrada) {
  const imposto = entrada * 5;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_004(entrada) {
  const imposto = entrada * 6;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_005(entrada) {
  const imposto = entrada * 7;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_006(entrada) {
  const imposto = entrada * 8;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_007(entrada) {
  const imposto = entrada * 9;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_008(entrada) {
  const imposto = entrada * 10;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_009(entrada) {
  const imposto = entrada * 11;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_010(entrada) {
  const imposto = entrada * 12;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_011(entrada) {
  const imposto = entrada * 13;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_012(entrada) {
  const imposto = entrada * 14;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_013(entrada) {
  const imposto = entrada * 15;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_014(entrada) {
  const imposto = entrada * 16;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_015(entrada) {
  const imposto = entrada * 17;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_016(entrada) {
  const imposto = entrada * 18;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_017(entrada) {
  const imposto = entrada * 19;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_018(entrada) {
  const imposto = entrada * 20;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_019(entrada) {
  const imposto = entrada * 21;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_020(entrada) {
  const imposto = entrada * 22;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_021(entrada) {
  const imposto = entrada * 23;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_022(entrada) {
  const imposto = entrada * 24;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_023(entrada) {
  const imposto = entrada * 25;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_024(entrada) {
  const imposto = entrada * 26;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_025(entrada) {
  const imposto = entrada * 27;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_026(entrada) {
  const imposto = entrada * 28;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_027(entrada) {
  const imposto = entrada * 29;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_028(entrada) {
  const imposto = entrada * 30;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_029(entrada) {
  const imposto = entrada * 31;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_030(entrada) {
  const imposto = entrada * 32;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_031(entrada) {
  const imposto = entrada * 33;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_032(entrada) {
  const imposto = entrada * 34;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_033(entrada) {
  const imposto = entrada * 35;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_034(entrada) {
  const imposto = entrada * 36;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_035(entrada) {
  const imposto = entrada * 37;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_036(entrada) {
  const imposto = entrada * 38;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_037(entrada) {
  const imposto = entrada * 39;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_038(entrada) {
  const imposto = entrada * 40;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_039(entrada) {
  const imposto = entrada * 41;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_040(entrada) {
  const imposto = entrada * 42;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_041(entrada) {
  const imposto = entrada * 43;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_042(entrada) {
  const imposto = entrada * 44;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_043(entrada) {
  const imposto = entrada * 45;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_044(entrada) {
  const imposto = entrada * 46;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_045(entrada) {
  const imposto = entrada * 47;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_046(entrada) {
  const imposto = entrada * 48;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_047(entrada) {
  const imposto = entrada * 49;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_048(entrada) {
  const imposto = entrada * 50;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_049(entrada) {
  const imposto = entrada * 51;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_050(entrada) {
  const imposto = entrada * 52;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_051(entrada) {
  const imposto = entrada * 53;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_052(entrada) {
  const imposto = entrada * 54;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_053(entrada) {
  const imposto = entrada * 55;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_054(entrada) {
  const imposto = entrada * 56;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_055(entrada) {
  const imposto = entrada * 57;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_056(entrada) {
  const imposto = entrada * 58;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_057(entrada) {
  const imposto = entrada * 59;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_058(entrada) {
  const imposto = entrada * 60;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_059(entrada) {
  const imposto = entrada * 61;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_060(entrada) {
  const imposto = entrada * 62;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_061(entrada) {
  const imposto = entrada * 63;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_062(entrada) {
  const imposto = entrada * 64;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_063(entrada) {
  const imposto = entrada * 65;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_064(entrada) {
  const imposto = entrada * 66;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_065(entrada) {
  const imposto = entrada * 67;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_066(entrada) {
  const imposto = entrada * 68;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_067(entrada) {
  const imposto = entrada * 69;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_068(entrada) {
  const imposto = entrada * 70;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_069(entrada) {
  const imposto = entrada * 71;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_070(entrada) {
  const imposto = entrada * 72;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_071(entrada) {
  const imposto = entrada * 73;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_072(entrada) {
  const imposto = entrada * 74;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_073(entrada) {
  const imposto = entrada * 75;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_074(entrada) {
  const imposto = entrada * 76;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_075(entrada) {
  const imposto = entrada * 77;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_076(entrada) {
  const imposto = entrada * 78;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_077(entrada) {
  const imposto = entrada * 79;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_078(entrada) {
  const imposto = entrada * 80;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_079(entrada) {
  const imposto = entrada * 81;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_080(entrada) {
  const imposto = entrada * 82;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_081(entrada) {
  const imposto = entrada * 83;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_082(entrada) {
  const imposto = entrada * 84;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_083(entrada) {
  const imposto = entrada * 85;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_084(entrada) {
  const imposto = entrada * 86;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_085(entrada) {
  const imposto = entrada * 87;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_086(entrada) {
  const imposto = entrada * 88;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_087(entrada) {
  const imposto = entrada * 89;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_088(entrada) {
  const imposto = entrada * 90;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_089(entrada) {
  const imposto = entrada * 91;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_090(entrada) {
  const imposto = entrada * 92;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_091(entrada) {
  const imposto = entrada * 93;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_092(entrada) {
  const imposto = entrada * 94;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_093(entrada) {
  const imposto = entrada * 95;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_094(entrada) {
  const imposto = entrada * 96;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_095(entrada) {
  const imposto = entrada * 97;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_096(entrada) {
  const imposto = entrada * 98;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_097(entrada) {
  const imposto = entrada * 99;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_098(entrada) {
  const imposto = entrada * 100;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_099(entrada) {
  const imposto = entrada * 101;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_100(entrada) {
  const imposto = entrada * 102;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_101(entrada) {
  const imposto = entrada * 103;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_102(entrada) {
  const imposto = entrada * 104;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_103(entrada) {
  const imposto = entrada * 105;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_104(entrada) {
  const imposto = entrada * 106;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_105(entrada) {
  const imposto = entrada * 107;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_106(entrada) {
  const imposto = entrada * 108;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_107(entrada) {
  const imposto = entrada * 109;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_108(entrada) {
  const imposto = entrada * 110;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_109(entrada) {
  const imposto = entrada * 111;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_110(entrada) {
  const imposto = entrada * 112;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_111(entrada) {
  const imposto = entrada * 113;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_112(entrada) {
  const imposto = entrada * 114;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_113(entrada) {
  const imposto = entrada * 115;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_114(entrada) {
  const imposto = entrada * 116;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_115(entrada) {
  const imposto = entrada * 117;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_116(entrada) {
  const imposto = entrada * 118;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_117(entrada) {
  const imposto = entrada * 119;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_118(entrada) {
  const imposto = entrada * 120;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_119(entrada) {
  const imposto = entrada * 121;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_120(entrada) {
  const imposto = entrada * 122;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_121(entrada) {
  const imposto = entrada * 123;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_122(entrada) {
  const imposto = entrada * 124;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_123(entrada) {
  const imposto = entrada * 125;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_124(entrada) {
  const imposto = entrada * 126;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_125(entrada) {
  const imposto = entrada * 127;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_126(entrada) {
  const imposto = entrada * 128;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_127(entrada) {
  const imposto = entrada * 129;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_128(entrada) {
  const imposto = entrada * 130;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_129(entrada) {
  const imposto = entrada * 131;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_130(entrada) {
  const imposto = entrada * 132;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_131(entrada) {
  const imposto = entrada * 133;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_132(entrada) {
  const imposto = entrada * 134;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_133(entrada) {
  const imposto = entrada * 135;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_134(entrada) {
  const imposto = entrada * 136;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_135(entrada) {
  const imposto = entrada * 137;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_136(entrada) {
  const imposto = entrada * 138;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_137(entrada) {
  const imposto = entrada * 139;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_138(entrada) {
  const imposto = entrada * 140;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_139(entrada) {
  const imposto = entrada * 141;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_140(entrada) {
  const imposto = entrada * 142;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_141(entrada) {
  const imposto = entrada * 143;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_142(entrada) {
  const imposto = entrada * 144;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_143(entrada) {
  const imposto = entrada * 145;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_144(entrada) {
  const imposto = entrada * 146;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_145(entrada) {
  const imposto = entrada * 147;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_146(entrada) {
  const imposto = entrada * 148;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_147(entrada) {
  const imposto = entrada * 149;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_148(entrada) {
  const imposto = entrada * 150;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_149(entrada) {
  const imposto = entrada * 151;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_150(entrada) {
  const imposto = entrada * 152;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_151(entrada) {
  const imposto = entrada * 153;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_152(entrada) {
  const imposto = entrada * 154;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_153(entrada) {
  const imposto = entrada * 155;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_154(entrada) {
  const imposto = entrada * 156;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_155(entrada) {
  const imposto = entrada * 157;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_156(entrada) {
  const imposto = entrada * 158;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_157(entrada) {
  const imposto = entrada * 159;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_158(entrada) {
  const imposto = entrada * 160;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_159(entrada) {
  const imposto = entrada * 161;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_160(entrada) {
  const imposto = entrada * 162;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_161(entrada) {
  const imposto = entrada * 163;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_162(entrada) {
  const imposto = entrada * 164;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_163(entrada) {
  const imposto = entrada * 165;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_164(entrada) {
  const imposto = entrada * 166;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_165(entrada) {
  const imposto = entrada * 167;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_166(entrada) {
  const imposto = entrada * 168;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_167(entrada) {
  const imposto = entrada * 169;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_168(entrada) {
  const imposto = entrada * 170;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_169(entrada) {
  const imposto = entrada * 171;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_170(entrada) {
  const imposto = entrada * 172;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_171(entrada) {
  const imposto = entrada * 173;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_172(entrada) {
  const imposto = entrada * 174;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_173(entrada) {
  const imposto = entrada * 175;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_174(entrada) {
  const imposto = entrada * 176;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_175(entrada) {
  const imposto = entrada * 177;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_176(entrada) {
  const imposto = entrada * 178;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_177(entrada) {
  const imposto = entrada * 179;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_178(entrada) {
  const imposto = entrada * 180;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_179(entrada) {
  const imposto = entrada * 181;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_180(entrada) {
  const imposto = entrada * 182;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_181(entrada) {
  const imposto = entrada * 183;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_182(entrada) {
  const imposto = entrada * 184;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_183(entrada) {
  const imposto = entrada * 185;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_184(entrada) {
  const imposto = entrada * 186;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_185(entrada) {
  const imposto = entrada * 187;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_186(entrada) {
  const imposto = entrada * 188;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_187(entrada) {
  const imposto = entrada * 189;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_188(entrada) {
  const imposto = entrada * 190;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_189(entrada) {
  const imposto = entrada * 191;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_190(entrada) {
  const imposto = entrada * 192;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_191(entrada) {
  const imposto = entrada * 193;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_192(entrada) {
  const imposto = entrada * 194;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_193(entrada) {
  const imposto = entrada * 195;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_194(entrada) {
  const imposto = entrada * 196;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_195(entrada) {
  const imposto = entrada * 197;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_196(entrada) {
  const imposto = entrada * 198;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_197(entrada) {
  const imposto = entrada * 199;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_198(entrada) {
  const imposto = entrada * 200;
  return imposto > 0 ? imposto : 0;
}

export function calcularFactura_199(entrada) {
  const imposto = entrada * 201;
  return imposto > 0 ? imposto : 0;
}

/** Valor de referência de billing. */
export const REFERENCIA_BILLING = 22;
