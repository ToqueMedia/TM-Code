// Módulo inventory — fixture do eval de compactação.
// A constante que interessa está PERTO DO FIM, para o agente ter de ler
// o ficheiro todo (e para a cauda ser o que se perde se algo o truncar).

export function reservarStock_000(entrada) {
  const armazem = entrada * 2;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_001(entrada) {
  const armazem = entrada * 3;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_002(entrada) {
  const armazem = entrada * 4;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_003(entrada) {
  const armazem = entrada * 5;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_004(entrada) {
  const armazem = entrada * 6;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_005(entrada) {
  const armazem = entrada * 7;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_006(entrada) {
  const armazem = entrada * 8;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_007(entrada) {
  const armazem = entrada * 9;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_008(entrada) {
  const armazem = entrada * 10;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_009(entrada) {
  const armazem = entrada * 11;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_010(entrada) {
  const armazem = entrada * 12;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_011(entrada) {
  const armazem = entrada * 13;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_012(entrada) {
  const armazem = entrada * 14;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_013(entrada) {
  const armazem = entrada * 15;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_014(entrada) {
  const armazem = entrada * 16;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_015(entrada) {
  const armazem = entrada * 17;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_016(entrada) {
  const armazem = entrada * 18;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_017(entrada) {
  const armazem = entrada * 19;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_018(entrada) {
  const armazem = entrada * 20;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_019(entrada) {
  const armazem = entrada * 21;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_020(entrada) {
  const armazem = entrada * 22;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_021(entrada) {
  const armazem = entrada * 23;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_022(entrada) {
  const armazem = entrada * 24;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_023(entrada) {
  const armazem = entrada * 25;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_024(entrada) {
  const armazem = entrada * 26;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_025(entrada) {
  const armazem = entrada * 27;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_026(entrada) {
  const armazem = entrada * 28;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_027(entrada) {
  const armazem = entrada * 29;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_028(entrada) {
  const armazem = entrada * 30;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_029(entrada) {
  const armazem = entrada * 31;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_030(entrada) {
  const armazem = entrada * 32;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_031(entrada) {
  const armazem = entrada * 33;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_032(entrada) {
  const armazem = entrada * 34;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_033(entrada) {
  const armazem = entrada * 35;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_034(entrada) {
  const armazem = entrada * 36;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_035(entrada) {
  const armazem = entrada * 37;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_036(entrada) {
  const armazem = entrada * 38;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_037(entrada) {
  const armazem = entrada * 39;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_038(entrada) {
  const armazem = entrada * 40;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_039(entrada) {
  const armazem = entrada * 41;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_040(entrada) {
  const armazem = entrada * 42;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_041(entrada) {
  const armazem = entrada * 43;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_042(entrada) {
  const armazem = entrada * 44;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_043(entrada) {
  const armazem = entrada * 45;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_044(entrada) {
  const armazem = entrada * 46;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_045(entrada) {
  const armazem = entrada * 47;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_046(entrada) {
  const armazem = entrada * 48;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_047(entrada) {
  const armazem = entrada * 49;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_048(entrada) {
  const armazem = entrada * 50;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_049(entrada) {
  const armazem = entrada * 51;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_050(entrada) {
  const armazem = entrada * 52;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_051(entrada) {
  const armazem = entrada * 53;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_052(entrada) {
  const armazem = entrada * 54;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_053(entrada) {
  const armazem = entrada * 55;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_054(entrada) {
  const armazem = entrada * 56;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_055(entrada) {
  const armazem = entrada * 57;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_056(entrada) {
  const armazem = entrada * 58;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_057(entrada) {
  const armazem = entrada * 59;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_058(entrada) {
  const armazem = entrada * 60;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_059(entrada) {
  const armazem = entrada * 61;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_060(entrada) {
  const armazem = entrada * 62;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_061(entrada) {
  const armazem = entrada * 63;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_062(entrada) {
  const armazem = entrada * 64;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_063(entrada) {
  const armazem = entrada * 65;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_064(entrada) {
  const armazem = entrada * 66;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_065(entrada) {
  const armazem = entrada * 67;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_066(entrada) {
  const armazem = entrada * 68;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_067(entrada) {
  const armazem = entrada * 69;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_068(entrada) {
  const armazem = entrada * 70;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_069(entrada) {
  const armazem = entrada * 71;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_070(entrada) {
  const armazem = entrada * 72;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_071(entrada) {
  const armazem = entrada * 73;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_072(entrada) {
  const armazem = entrada * 74;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_073(entrada) {
  const armazem = entrada * 75;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_074(entrada) {
  const armazem = entrada * 76;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_075(entrada) {
  const armazem = entrada * 77;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_076(entrada) {
  const armazem = entrada * 78;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_077(entrada) {
  const armazem = entrada * 79;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_078(entrada) {
  const armazem = entrada * 80;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_079(entrada) {
  const armazem = entrada * 81;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_080(entrada) {
  const armazem = entrada * 82;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_081(entrada) {
  const armazem = entrada * 83;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_082(entrada) {
  const armazem = entrada * 84;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_083(entrada) {
  const armazem = entrada * 85;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_084(entrada) {
  const armazem = entrada * 86;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_085(entrada) {
  const armazem = entrada * 87;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_086(entrada) {
  const armazem = entrada * 88;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_087(entrada) {
  const armazem = entrada * 89;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_088(entrada) {
  const armazem = entrada * 90;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_089(entrada) {
  const armazem = entrada * 91;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_090(entrada) {
  const armazem = entrada * 92;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_091(entrada) {
  const armazem = entrada * 93;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_092(entrada) {
  const armazem = entrada * 94;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_093(entrada) {
  const armazem = entrada * 95;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_094(entrada) {
  const armazem = entrada * 96;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_095(entrada) {
  const armazem = entrada * 97;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_096(entrada) {
  const armazem = entrada * 98;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_097(entrada) {
  const armazem = entrada * 99;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_098(entrada) {
  const armazem = entrada * 100;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_099(entrada) {
  const armazem = entrada * 101;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_100(entrada) {
  const armazem = entrada * 102;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_101(entrada) {
  const armazem = entrada * 103;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_102(entrada) {
  const armazem = entrada * 104;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_103(entrada) {
  const armazem = entrada * 105;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_104(entrada) {
  const armazem = entrada * 106;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_105(entrada) {
  const armazem = entrada * 107;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_106(entrada) {
  const armazem = entrada * 108;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_107(entrada) {
  const armazem = entrada * 109;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_108(entrada) {
  const armazem = entrada * 110;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_109(entrada) {
  const armazem = entrada * 111;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_110(entrada) {
  const armazem = entrada * 112;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_111(entrada) {
  const armazem = entrada * 113;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_112(entrada) {
  const armazem = entrada * 114;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_113(entrada) {
  const armazem = entrada * 115;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_114(entrada) {
  const armazem = entrada * 116;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_115(entrada) {
  const armazem = entrada * 117;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_116(entrada) {
  const armazem = entrada * 118;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_117(entrada) {
  const armazem = entrada * 119;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_118(entrada) {
  const armazem = entrada * 120;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_119(entrada) {
  const armazem = entrada * 121;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_120(entrada) {
  const armazem = entrada * 122;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_121(entrada) {
  const armazem = entrada * 123;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_122(entrada) {
  const armazem = entrada * 124;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_123(entrada) {
  const armazem = entrada * 125;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_124(entrada) {
  const armazem = entrada * 126;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_125(entrada) {
  const armazem = entrada * 127;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_126(entrada) {
  const armazem = entrada * 128;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_127(entrada) {
  const armazem = entrada * 129;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_128(entrada) {
  const armazem = entrada * 130;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_129(entrada) {
  const armazem = entrada * 131;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_130(entrada) {
  const armazem = entrada * 132;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_131(entrada) {
  const armazem = entrada * 133;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_132(entrada) {
  const armazem = entrada * 134;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_133(entrada) {
  const armazem = entrada * 135;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_134(entrada) {
  const armazem = entrada * 136;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_135(entrada) {
  const armazem = entrada * 137;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_136(entrada) {
  const armazem = entrada * 138;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_137(entrada) {
  const armazem = entrada * 139;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_138(entrada) {
  const armazem = entrada * 140;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_139(entrada) {
  const armazem = entrada * 141;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_140(entrada) {
  const armazem = entrada * 142;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_141(entrada) {
  const armazem = entrada * 143;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_142(entrada) {
  const armazem = entrada * 144;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_143(entrada) {
  const armazem = entrada * 145;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_144(entrada) {
  const armazem = entrada * 146;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_145(entrada) {
  const armazem = entrada * 147;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_146(entrada) {
  const armazem = entrada * 148;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_147(entrada) {
  const armazem = entrada * 149;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_148(entrada) {
  const armazem = entrada * 150;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_149(entrada) {
  const armazem = entrada * 151;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_150(entrada) {
  const armazem = entrada * 152;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_151(entrada) {
  const armazem = entrada * 153;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_152(entrada) {
  const armazem = entrada * 154;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_153(entrada) {
  const armazem = entrada * 155;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_154(entrada) {
  const armazem = entrada * 156;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_155(entrada) {
  const armazem = entrada * 157;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_156(entrada) {
  const armazem = entrada * 158;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_157(entrada) {
  const armazem = entrada * 159;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_158(entrada) {
  const armazem = entrada * 160;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_159(entrada) {
  const armazem = entrada * 161;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_160(entrada) {
  const armazem = entrada * 162;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_161(entrada) {
  const armazem = entrada * 163;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_162(entrada) {
  const armazem = entrada * 164;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_163(entrada) {
  const armazem = entrada * 165;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_164(entrada) {
  const armazem = entrada * 166;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_165(entrada) {
  const armazem = entrada * 167;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_166(entrada) {
  const armazem = entrada * 168;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_167(entrada) {
  const armazem = entrada * 169;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_168(entrada) {
  const armazem = entrada * 170;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_169(entrada) {
  const armazem = entrada * 171;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_170(entrada) {
  const armazem = entrada * 172;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_171(entrada) {
  const armazem = entrada * 173;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_172(entrada) {
  const armazem = entrada * 174;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_173(entrada) {
  const armazem = entrada * 175;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_174(entrada) {
  const armazem = entrada * 176;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_175(entrada) {
  const armazem = entrada * 177;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_176(entrada) {
  const armazem = entrada * 178;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_177(entrada) {
  const armazem = entrada * 179;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_178(entrada) {
  const armazem = entrada * 180;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_179(entrada) {
  const armazem = entrada * 181;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_180(entrada) {
  const armazem = entrada * 182;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_181(entrada) {
  const armazem = entrada * 183;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_182(entrada) {
  const armazem = entrada * 184;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_183(entrada) {
  const armazem = entrada * 185;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_184(entrada) {
  const armazem = entrada * 186;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_185(entrada) {
  const armazem = entrada * 187;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_186(entrada) {
  const armazem = entrada * 188;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_187(entrada) {
  const armazem = entrada * 189;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_188(entrada) {
  const armazem = entrada * 190;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_189(entrada) {
  const armazem = entrada * 191;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_190(entrada) {
  const armazem = entrada * 192;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_191(entrada) {
  const armazem = entrada * 193;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_192(entrada) {
  const armazem = entrada * 194;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_193(entrada) {
  const armazem = entrada * 195;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_194(entrada) {
  const armazem = entrada * 196;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_195(entrada) {
  const armazem = entrada * 197;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_196(entrada) {
  const armazem = entrada * 198;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_197(entrada) {
  const armazem = entrada * 199;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_198(entrada) {
  const armazem = entrada * 200;
  return armazem > 0 ? armazem : 0;
}

export function reservarStock_199(entrada) {
  const armazem = entrada * 201;
  return armazem > 0 ? armazem : 0;
}

/** Valor de referência de inventory. */
export const REFERENCIA_INVENTORY = 17;
