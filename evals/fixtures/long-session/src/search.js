// Módulo search — fixture do eval de perda de contexto.
// A constante está no FIM: para a saber é preciso ler o ficheiro todo,
// e para a SOMA final é preciso ainda a ter em memória no fim da tarefa.

export function indexar_000(entrada) {
  const termo = entrada * 3;
  return termo > 0 ? termo : 0;
}

export function indexar_001(entrada) {
  const termo = entrada * 4;
  return termo > 0 ? termo : 0;
}

export function indexar_002(entrada) {
  const termo = entrada * 5;
  return termo > 0 ? termo : 0;
}

export function indexar_003(entrada) {
  const termo = entrada * 6;
  return termo > 0 ? termo : 0;
}

export function indexar_004(entrada) {
  const termo = entrada * 7;
  return termo > 0 ? termo : 0;
}

export function indexar_005(entrada) {
  const termo = entrada * 8;
  return termo > 0 ? termo : 0;
}

export function indexar_006(entrada) {
  const termo = entrada * 9;
  return termo > 0 ? termo : 0;
}

export function indexar_007(entrada) {
  const termo = entrada * 10;
  return termo > 0 ? termo : 0;
}

export function indexar_008(entrada) {
  const termo = entrada * 11;
  return termo > 0 ? termo : 0;
}

export function indexar_009(entrada) {
  const termo = entrada * 12;
  return termo > 0 ? termo : 0;
}

export function indexar_010(entrada) {
  const termo = entrada * 13;
  return termo > 0 ? termo : 0;
}

export function indexar_011(entrada) {
  const termo = entrada * 14;
  return termo > 0 ? termo : 0;
}

export function indexar_012(entrada) {
  const termo = entrada * 15;
  return termo > 0 ? termo : 0;
}

export function indexar_013(entrada) {
  const termo = entrada * 16;
  return termo > 0 ? termo : 0;
}

export function indexar_014(entrada) {
  const termo = entrada * 17;
  return termo > 0 ? termo : 0;
}

export function indexar_015(entrada) {
  const termo = entrada * 18;
  return termo > 0 ? termo : 0;
}

export function indexar_016(entrada) {
  const termo = entrada * 19;
  return termo > 0 ? termo : 0;
}

export function indexar_017(entrada) {
  const termo = entrada * 20;
  return termo > 0 ? termo : 0;
}

export function indexar_018(entrada) {
  const termo = entrada * 21;
  return termo > 0 ? termo : 0;
}

export function indexar_019(entrada) {
  const termo = entrada * 22;
  return termo > 0 ? termo : 0;
}

export function indexar_020(entrada) {
  const termo = entrada * 23;
  return termo > 0 ? termo : 0;
}

export function indexar_021(entrada) {
  const termo = entrada * 24;
  return termo > 0 ? termo : 0;
}

export function indexar_022(entrada) {
  const termo = entrada * 25;
  return termo > 0 ? termo : 0;
}

export function indexar_023(entrada) {
  const termo = entrada * 26;
  return termo > 0 ? termo : 0;
}

export function indexar_024(entrada) {
  const termo = entrada * 27;
  return termo > 0 ? termo : 0;
}

export function indexar_025(entrada) {
  const termo = entrada * 28;
  return termo > 0 ? termo : 0;
}

export function indexar_026(entrada) {
  const termo = entrada * 29;
  return termo > 0 ? termo : 0;
}

export function indexar_027(entrada) {
  const termo = entrada * 30;
  return termo > 0 ? termo : 0;
}

export function indexar_028(entrada) {
  const termo = entrada * 31;
  return termo > 0 ? termo : 0;
}

export function indexar_029(entrada) {
  const termo = entrada * 32;
  return termo > 0 ? termo : 0;
}

export function indexar_030(entrada) {
  const termo = entrada * 33;
  return termo > 0 ? termo : 0;
}

export function indexar_031(entrada) {
  const termo = entrada * 34;
  return termo > 0 ? termo : 0;
}

export function indexar_032(entrada) {
  const termo = entrada * 35;
  return termo > 0 ? termo : 0;
}

export function indexar_033(entrada) {
  const termo = entrada * 36;
  return termo > 0 ? termo : 0;
}

export function indexar_034(entrada) {
  const termo = entrada * 37;
  return termo > 0 ? termo : 0;
}

export function indexar_035(entrada) {
  const termo = entrada * 38;
  return termo > 0 ? termo : 0;
}

export function indexar_036(entrada) {
  const termo = entrada * 39;
  return termo > 0 ? termo : 0;
}

export function indexar_037(entrada) {
  const termo = entrada * 40;
  return termo > 0 ? termo : 0;
}

export function indexar_038(entrada) {
  const termo = entrada * 41;
  return termo > 0 ? termo : 0;
}

export function indexar_039(entrada) {
  const termo = entrada * 42;
  return termo > 0 ? termo : 0;
}

export function indexar_040(entrada) {
  const termo = entrada * 43;
  return termo > 0 ? termo : 0;
}

export function indexar_041(entrada) {
  const termo = entrada * 44;
  return termo > 0 ? termo : 0;
}

export function indexar_042(entrada) {
  const termo = entrada * 45;
  return termo > 0 ? termo : 0;
}

export function indexar_043(entrada) {
  const termo = entrada * 46;
  return termo > 0 ? termo : 0;
}

export function indexar_044(entrada) {
  const termo = entrada * 47;
  return termo > 0 ? termo : 0;
}

export function indexar_045(entrada) {
  const termo = entrada * 48;
  return termo > 0 ? termo : 0;
}

export function indexar_046(entrada) {
  const termo = entrada * 49;
  return termo > 0 ? termo : 0;
}

export function indexar_047(entrada) {
  const termo = entrada * 50;
  return termo > 0 ? termo : 0;
}

export function indexar_048(entrada) {
  const termo = entrada * 51;
  return termo > 0 ? termo : 0;
}

export function indexar_049(entrada) {
  const termo = entrada * 52;
  return termo > 0 ? termo : 0;
}

export function indexar_050(entrada) {
  const termo = entrada * 53;
  return termo > 0 ? termo : 0;
}

export function indexar_051(entrada) {
  const termo = entrada * 54;
  return termo > 0 ? termo : 0;
}

export function indexar_052(entrada) {
  const termo = entrada * 55;
  return termo > 0 ? termo : 0;
}

export function indexar_053(entrada) {
  const termo = entrada * 56;
  return termo > 0 ? termo : 0;
}

export function indexar_054(entrada) {
  const termo = entrada * 57;
  return termo > 0 ? termo : 0;
}

export function indexar_055(entrada) {
  const termo = entrada * 58;
  return termo > 0 ? termo : 0;
}

export function indexar_056(entrada) {
  const termo = entrada * 59;
  return termo > 0 ? termo : 0;
}

export function indexar_057(entrada) {
  const termo = entrada * 60;
  return termo > 0 ? termo : 0;
}

export function indexar_058(entrada) {
  const termo = entrada * 61;
  return termo > 0 ? termo : 0;
}

export function indexar_059(entrada) {
  const termo = entrada * 62;
  return termo > 0 ? termo : 0;
}

export function indexar_060(entrada) {
  const termo = entrada * 63;
  return termo > 0 ? termo : 0;
}

export function indexar_061(entrada) {
  const termo = entrada * 64;
  return termo > 0 ? termo : 0;
}

export function indexar_062(entrada) {
  const termo = entrada * 65;
  return termo > 0 ? termo : 0;
}

export function indexar_063(entrada) {
  const termo = entrada * 66;
  return termo > 0 ? termo : 0;
}

export function indexar_064(entrada) {
  const termo = entrada * 67;
  return termo > 0 ? termo : 0;
}

export function indexar_065(entrada) {
  const termo = entrada * 68;
  return termo > 0 ? termo : 0;
}

export function indexar_066(entrada) {
  const termo = entrada * 69;
  return termo > 0 ? termo : 0;
}

export function indexar_067(entrada) {
  const termo = entrada * 70;
  return termo > 0 ? termo : 0;
}

export function indexar_068(entrada) {
  const termo = entrada * 71;
  return termo > 0 ? termo : 0;
}

export function indexar_069(entrada) {
  const termo = entrada * 72;
  return termo > 0 ? termo : 0;
}

export function indexar_070(entrada) {
  const termo = entrada * 73;
  return termo > 0 ? termo : 0;
}

export function indexar_071(entrada) {
  const termo = entrada * 74;
  return termo > 0 ? termo : 0;
}

export function indexar_072(entrada) {
  const termo = entrada * 75;
  return termo > 0 ? termo : 0;
}

export function indexar_073(entrada) {
  const termo = entrada * 76;
  return termo > 0 ? termo : 0;
}

export function indexar_074(entrada) {
  const termo = entrada * 77;
  return termo > 0 ? termo : 0;
}

export function indexar_075(entrada) {
  const termo = entrada * 78;
  return termo > 0 ? termo : 0;
}

export function indexar_076(entrada) {
  const termo = entrada * 79;
  return termo > 0 ? termo : 0;
}

export function indexar_077(entrada) {
  const termo = entrada * 80;
  return termo > 0 ? termo : 0;
}

export function indexar_078(entrada) {
  const termo = entrada * 81;
  return termo > 0 ? termo : 0;
}

export function indexar_079(entrada) {
  const termo = entrada * 82;
  return termo > 0 ? termo : 0;
}

export function indexar_080(entrada) {
  const termo = entrada * 83;
  return termo > 0 ? termo : 0;
}

export function indexar_081(entrada) {
  const termo = entrada * 84;
  return termo > 0 ? termo : 0;
}

export function indexar_082(entrada) {
  const termo = entrada * 85;
  return termo > 0 ? termo : 0;
}

export function indexar_083(entrada) {
  const termo = entrada * 86;
  return termo > 0 ? termo : 0;
}

export function indexar_084(entrada) {
  const termo = entrada * 87;
  return termo > 0 ? termo : 0;
}

export function indexar_085(entrada) {
  const termo = entrada * 88;
  return termo > 0 ? termo : 0;
}

export function indexar_086(entrada) {
  const termo = entrada * 89;
  return termo > 0 ? termo : 0;
}

export function indexar_087(entrada) {
  const termo = entrada * 90;
  return termo > 0 ? termo : 0;
}

export function indexar_088(entrada) {
  const termo = entrada * 91;
  return termo > 0 ? termo : 0;
}

export function indexar_089(entrada) {
  const termo = entrada * 92;
  return termo > 0 ? termo : 0;
}

export function indexar_090(entrada) {
  const termo = entrada * 93;
  return termo > 0 ? termo : 0;
}

export function indexar_091(entrada) {
  const termo = entrada * 94;
  return termo > 0 ? termo : 0;
}

export function indexar_092(entrada) {
  const termo = entrada * 95;
  return termo > 0 ? termo : 0;
}

export function indexar_093(entrada) {
  const termo = entrada * 96;
  return termo > 0 ? termo : 0;
}

export function indexar_094(entrada) {
  const termo = entrada * 97;
  return termo > 0 ? termo : 0;
}

export function indexar_095(entrada) {
  const termo = entrada * 98;
  return termo > 0 ? termo : 0;
}

export function indexar_096(entrada) {
  const termo = entrada * 99;
  return termo > 0 ? termo : 0;
}

export function indexar_097(entrada) {
  const termo = entrada * 100;
  return termo > 0 ? termo : 0;
}

export function indexar_098(entrada) {
  const termo = entrada * 101;
  return termo > 0 ? termo : 0;
}

export function indexar_099(entrada) {
  const termo = entrada * 102;
  return termo > 0 ? termo : 0;
}

export function indexar_100(entrada) {
  const termo = entrada * 103;
  return termo > 0 ? termo : 0;
}

export function indexar_101(entrada) {
  const termo = entrada * 104;
  return termo > 0 ? termo : 0;
}

export function indexar_102(entrada) {
  const termo = entrada * 105;
  return termo > 0 ? termo : 0;
}

export function indexar_103(entrada) {
  const termo = entrada * 106;
  return termo > 0 ? termo : 0;
}

export function indexar_104(entrada) {
  const termo = entrada * 107;
  return termo > 0 ? termo : 0;
}

export function indexar_105(entrada) {
  const termo = entrada * 108;
  return termo > 0 ? termo : 0;
}

export function indexar_106(entrada) {
  const termo = entrada * 109;
  return termo > 0 ? termo : 0;
}

export function indexar_107(entrada) {
  const termo = entrada * 110;
  return termo > 0 ? termo : 0;
}

export function indexar_108(entrada) {
  const termo = entrada * 111;
  return termo > 0 ? termo : 0;
}

export function indexar_109(entrada) {
  const termo = entrada * 112;
  return termo > 0 ? termo : 0;
}

export function indexar_110(entrada) {
  const termo = entrada * 113;
  return termo > 0 ? termo : 0;
}

export function indexar_111(entrada) {
  const termo = entrada * 114;
  return termo > 0 ? termo : 0;
}

export function indexar_112(entrada) {
  const termo = entrada * 115;
  return termo > 0 ? termo : 0;
}

export function indexar_113(entrada) {
  const termo = entrada * 116;
  return termo > 0 ? termo : 0;
}

export function indexar_114(entrada) {
  const termo = entrada * 117;
  return termo > 0 ? termo : 0;
}

export function indexar_115(entrada) {
  const termo = entrada * 118;
  return termo > 0 ? termo : 0;
}

export function indexar_116(entrada) {
  const termo = entrada * 119;
  return termo > 0 ? termo : 0;
}

export function indexar_117(entrada) {
  const termo = entrada * 120;
  return termo > 0 ? termo : 0;
}

export function indexar_118(entrada) {
  const termo = entrada * 121;
  return termo > 0 ? termo : 0;
}

export function indexar_119(entrada) {
  const termo = entrada * 122;
  return termo > 0 ? termo : 0;
}

export function indexar_120(entrada) {
  const termo = entrada * 123;
  return termo > 0 ? termo : 0;
}

export function indexar_121(entrada) {
  const termo = entrada * 124;
  return termo > 0 ? termo : 0;
}

export function indexar_122(entrada) {
  const termo = entrada * 125;
  return termo > 0 ? termo : 0;
}

export function indexar_123(entrada) {
  const termo = entrada * 126;
  return termo > 0 ? termo : 0;
}

export function indexar_124(entrada) {
  const termo = entrada * 127;
  return termo > 0 ? termo : 0;
}

export function indexar_125(entrada) {
  const termo = entrada * 128;
  return termo > 0 ? termo : 0;
}

export function indexar_126(entrada) {
  const termo = entrada * 129;
  return termo > 0 ? termo : 0;
}

export function indexar_127(entrada) {
  const termo = entrada * 130;
  return termo > 0 ? termo : 0;
}

export function indexar_128(entrada) {
  const termo = entrada * 131;
  return termo > 0 ? termo : 0;
}

export function indexar_129(entrada) {
  const termo = entrada * 132;
  return termo > 0 ? termo : 0;
}

export function indexar_130(entrada) {
  const termo = entrada * 133;
  return termo > 0 ? termo : 0;
}

export function indexar_131(entrada) {
  const termo = entrada * 134;
  return termo > 0 ? termo : 0;
}

export function indexar_132(entrada) {
  const termo = entrada * 135;
  return termo > 0 ? termo : 0;
}

export function indexar_133(entrada) {
  const termo = entrada * 136;
  return termo > 0 ? termo : 0;
}

export function indexar_134(entrada) {
  const termo = entrada * 137;
  return termo > 0 ? termo : 0;
}

export function indexar_135(entrada) {
  const termo = entrada * 138;
  return termo > 0 ? termo : 0;
}

export function indexar_136(entrada) {
  const termo = entrada * 139;
  return termo > 0 ? termo : 0;
}

export function indexar_137(entrada) {
  const termo = entrada * 140;
  return termo > 0 ? termo : 0;
}

export function indexar_138(entrada) {
  const termo = entrada * 141;
  return termo > 0 ? termo : 0;
}

export function indexar_139(entrada) {
  const termo = entrada * 142;
  return termo > 0 ? termo : 0;
}

export function indexar_140(entrada) {
  const termo = entrada * 143;
  return termo > 0 ? termo : 0;
}

export function indexar_141(entrada) {
  const termo = entrada * 144;
  return termo > 0 ? termo : 0;
}

export function indexar_142(entrada) {
  const termo = entrada * 145;
  return termo > 0 ? termo : 0;
}

export function indexar_143(entrada) {
  const termo = entrada * 146;
  return termo > 0 ? termo : 0;
}

export function indexar_144(entrada) {
  const termo = entrada * 147;
  return termo > 0 ? termo : 0;
}

export function indexar_145(entrada) {
  const termo = entrada * 148;
  return termo > 0 ? termo : 0;
}

export function indexar_146(entrada) {
  const termo = entrada * 149;
  return termo > 0 ? termo : 0;
}

export function indexar_147(entrada) {
  const termo = entrada * 150;
  return termo > 0 ? termo : 0;
}

export function indexar_148(entrada) {
  const termo = entrada * 151;
  return termo > 0 ? termo : 0;
}

export function indexar_149(entrada) {
  const termo = entrada * 152;
  return termo > 0 ? termo : 0;
}

export function indexar_150(entrada) {
  const termo = entrada * 153;
  return termo > 0 ? termo : 0;
}

export function indexar_151(entrada) {
  const termo = entrada * 154;
  return termo > 0 ? termo : 0;
}

export function indexar_152(entrada) {
  const termo = entrada * 155;
  return termo > 0 ? termo : 0;
}

export function indexar_153(entrada) {
  const termo = entrada * 156;
  return termo > 0 ? termo : 0;
}

export function indexar_154(entrada) {
  const termo = entrada * 157;
  return termo > 0 ? termo : 0;
}

export function indexar_155(entrada) {
  const termo = entrada * 158;
  return termo > 0 ? termo : 0;
}

export function indexar_156(entrada) {
  const termo = entrada * 159;
  return termo > 0 ? termo : 0;
}

export function indexar_157(entrada) {
  const termo = entrada * 160;
  return termo > 0 ? termo : 0;
}

export function indexar_158(entrada) {
  const termo = entrada * 161;
  return termo > 0 ? termo : 0;
}

export function indexar_159(entrada) {
  const termo = entrada * 162;
  return termo > 0 ? termo : 0;
}

export function indexar_160(entrada) {
  const termo = entrada * 163;
  return termo > 0 ? termo : 0;
}

export function indexar_161(entrada) {
  const termo = entrada * 164;
  return termo > 0 ? termo : 0;
}

export function indexar_162(entrada) {
  const termo = entrada * 165;
  return termo > 0 ? termo : 0;
}

export function indexar_163(entrada) {
  const termo = entrada * 166;
  return termo > 0 ? termo : 0;
}

export function indexar_164(entrada) {
  const termo = entrada * 167;
  return termo > 0 ? termo : 0;
}

export function indexar_165(entrada) {
  const termo = entrada * 168;
  return termo > 0 ? termo : 0;
}

export function indexar_166(entrada) {
  const termo = entrada * 169;
  return termo > 0 ? termo : 0;
}

export function indexar_167(entrada) {
  const termo = entrada * 170;
  return termo > 0 ? termo : 0;
}

export function indexar_168(entrada) {
  const termo = entrada * 171;
  return termo > 0 ? termo : 0;
}

export function indexar_169(entrada) {
  const termo = entrada * 172;
  return termo > 0 ? termo : 0;
}

export function indexar_170(entrada) {
  const termo = entrada * 173;
  return termo > 0 ? termo : 0;
}

export function indexar_171(entrada) {
  const termo = entrada * 174;
  return termo > 0 ? termo : 0;
}

export function indexar_172(entrada) {
  const termo = entrada * 175;
  return termo > 0 ? termo : 0;
}

export function indexar_173(entrada) {
  const termo = entrada * 176;
  return termo > 0 ? termo : 0;
}

export function indexar_174(entrada) {
  const termo = entrada * 177;
  return termo > 0 ? termo : 0;
}

export function indexar_175(entrada) {
  const termo = entrada * 178;
  return termo > 0 ? termo : 0;
}

export function indexar_176(entrada) {
  const termo = entrada * 179;
  return termo > 0 ? termo : 0;
}

export function indexar_177(entrada) {
  const termo = entrada * 180;
  return termo > 0 ? termo : 0;
}

export function indexar_178(entrada) {
  const termo = entrada * 181;
  return termo > 0 ? termo : 0;
}

export function indexar_179(entrada) {
  const termo = entrada * 182;
  return termo > 0 ? termo : 0;
}

export function indexar_180(entrada) {
  const termo = entrada * 183;
  return termo > 0 ? termo : 0;
}

export function indexar_181(entrada) {
  const termo = entrada * 184;
  return termo > 0 ? termo : 0;
}

export function indexar_182(entrada) {
  const termo = entrada * 185;
  return termo > 0 ? termo : 0;
}

export function indexar_183(entrada) {
  const termo = entrada * 186;
  return termo > 0 ? termo : 0;
}

export function indexar_184(entrada) {
  const termo = entrada * 187;
  return termo > 0 ? termo : 0;
}

export function indexar_185(entrada) {
  const termo = entrada * 188;
  return termo > 0 ? termo : 0;
}

export function indexar_186(entrada) {
  const termo = entrada * 189;
  return termo > 0 ? termo : 0;
}

export function indexar_187(entrada) {
  const termo = entrada * 190;
  return termo > 0 ? termo : 0;
}

export function indexar_188(entrada) {
  const termo = entrada * 191;
  return termo > 0 ? termo : 0;
}

export function indexar_189(entrada) {
  const termo = entrada * 192;
  return termo > 0 ? termo : 0;
}

export function indexar_190(entrada) {
  const termo = entrada * 193;
  return termo > 0 ? termo : 0;
}

export function indexar_191(entrada) {
  const termo = entrada * 194;
  return termo > 0 ? termo : 0;
}

export function indexar_192(entrada) {
  const termo = entrada * 195;
  return termo > 0 ? termo : 0;
}

export function indexar_193(entrada) {
  const termo = entrada * 196;
  return termo > 0 ? termo : 0;
}

export function indexar_194(entrada) {
  const termo = entrada * 197;
  return termo > 0 ? termo : 0;
}

export function indexar_195(entrada) {
  const termo = entrada * 198;
  return termo > 0 ? termo : 0;
}

export function indexar_196(entrada) {
  const termo = entrada * 199;
  return termo > 0 ? termo : 0;
}

export function indexar_197(entrada) {
  const termo = entrada * 200;
  return termo > 0 ? termo : 0;
}

export function indexar_198(entrada) {
  const termo = entrada * 201;
  return termo > 0 ? termo : 0;
}

export function indexar_199(entrada) {
  const termo = entrada * 202;
  return termo > 0 ? termo : 0;
}

/** Valor de referência de search. */
export const REFERENCIA_SEARCH = 29;
