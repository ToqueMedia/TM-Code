// Módulo report — fixture do eval de perda de contexto.
// A constante está no FIM: para a saber é preciso ler o ficheiro todo,
// e para a SOMA final é preciso ainda a ter em memória no fim da tarefa.

export function agregar_000(entrada) {
  const linha = entrada * 3;
  return linha > 0 ? linha : 0;
}

export function agregar_001(entrada) {
  const linha = entrada * 4;
  return linha > 0 ? linha : 0;
}

export function agregar_002(entrada) {
  const linha = entrada * 5;
  return linha > 0 ? linha : 0;
}

export function agregar_003(entrada) {
  const linha = entrada * 6;
  return linha > 0 ? linha : 0;
}

export function agregar_004(entrada) {
  const linha = entrada * 7;
  return linha > 0 ? linha : 0;
}

export function agregar_005(entrada) {
  const linha = entrada * 8;
  return linha > 0 ? linha : 0;
}

export function agregar_006(entrada) {
  const linha = entrada * 9;
  return linha > 0 ? linha : 0;
}

export function agregar_007(entrada) {
  const linha = entrada * 10;
  return linha > 0 ? linha : 0;
}

export function agregar_008(entrada) {
  const linha = entrada * 11;
  return linha > 0 ? linha : 0;
}

export function agregar_009(entrada) {
  const linha = entrada * 12;
  return linha > 0 ? linha : 0;
}

export function agregar_010(entrada) {
  const linha = entrada * 13;
  return linha > 0 ? linha : 0;
}

export function agregar_011(entrada) {
  const linha = entrada * 14;
  return linha > 0 ? linha : 0;
}

export function agregar_012(entrada) {
  const linha = entrada * 15;
  return linha > 0 ? linha : 0;
}

export function agregar_013(entrada) {
  const linha = entrada * 16;
  return linha > 0 ? linha : 0;
}

export function agregar_014(entrada) {
  const linha = entrada * 17;
  return linha > 0 ? linha : 0;
}

export function agregar_015(entrada) {
  const linha = entrada * 18;
  return linha > 0 ? linha : 0;
}

export function agregar_016(entrada) {
  const linha = entrada * 19;
  return linha > 0 ? linha : 0;
}

export function agregar_017(entrada) {
  const linha = entrada * 20;
  return linha > 0 ? linha : 0;
}

export function agregar_018(entrada) {
  const linha = entrada * 21;
  return linha > 0 ? linha : 0;
}

export function agregar_019(entrada) {
  const linha = entrada * 22;
  return linha > 0 ? linha : 0;
}

export function agregar_020(entrada) {
  const linha = entrada * 23;
  return linha > 0 ? linha : 0;
}

export function agregar_021(entrada) {
  const linha = entrada * 24;
  return linha > 0 ? linha : 0;
}

export function agregar_022(entrada) {
  const linha = entrada * 25;
  return linha > 0 ? linha : 0;
}

export function agregar_023(entrada) {
  const linha = entrada * 26;
  return linha > 0 ? linha : 0;
}

export function agregar_024(entrada) {
  const linha = entrada * 27;
  return linha > 0 ? linha : 0;
}

export function agregar_025(entrada) {
  const linha = entrada * 28;
  return linha > 0 ? linha : 0;
}

export function agregar_026(entrada) {
  const linha = entrada * 29;
  return linha > 0 ? linha : 0;
}

export function agregar_027(entrada) {
  const linha = entrada * 30;
  return linha > 0 ? linha : 0;
}

export function agregar_028(entrada) {
  const linha = entrada * 31;
  return linha > 0 ? linha : 0;
}

export function agregar_029(entrada) {
  const linha = entrada * 32;
  return linha > 0 ? linha : 0;
}

export function agregar_030(entrada) {
  const linha = entrada * 33;
  return linha > 0 ? linha : 0;
}

export function agregar_031(entrada) {
  const linha = entrada * 34;
  return linha > 0 ? linha : 0;
}

export function agregar_032(entrada) {
  const linha = entrada * 35;
  return linha > 0 ? linha : 0;
}

export function agregar_033(entrada) {
  const linha = entrada * 36;
  return linha > 0 ? linha : 0;
}

export function agregar_034(entrada) {
  const linha = entrada * 37;
  return linha > 0 ? linha : 0;
}

export function agregar_035(entrada) {
  const linha = entrada * 38;
  return linha > 0 ? linha : 0;
}

export function agregar_036(entrada) {
  const linha = entrada * 39;
  return linha > 0 ? linha : 0;
}

export function agregar_037(entrada) {
  const linha = entrada * 40;
  return linha > 0 ? linha : 0;
}

export function agregar_038(entrada) {
  const linha = entrada * 41;
  return linha > 0 ? linha : 0;
}

export function agregar_039(entrada) {
  const linha = entrada * 42;
  return linha > 0 ? linha : 0;
}

export function agregar_040(entrada) {
  const linha = entrada * 43;
  return linha > 0 ? linha : 0;
}

export function agregar_041(entrada) {
  const linha = entrada * 44;
  return linha > 0 ? linha : 0;
}

export function agregar_042(entrada) {
  const linha = entrada * 45;
  return linha > 0 ? linha : 0;
}

export function agregar_043(entrada) {
  const linha = entrada * 46;
  return linha > 0 ? linha : 0;
}

export function agregar_044(entrada) {
  const linha = entrada * 47;
  return linha > 0 ? linha : 0;
}

export function agregar_045(entrada) {
  const linha = entrada * 48;
  return linha > 0 ? linha : 0;
}

export function agregar_046(entrada) {
  const linha = entrada * 49;
  return linha > 0 ? linha : 0;
}

export function agregar_047(entrada) {
  const linha = entrada * 50;
  return linha > 0 ? linha : 0;
}

export function agregar_048(entrada) {
  const linha = entrada * 51;
  return linha > 0 ? linha : 0;
}

export function agregar_049(entrada) {
  const linha = entrada * 52;
  return linha > 0 ? linha : 0;
}

export function agregar_050(entrada) {
  const linha = entrada * 53;
  return linha > 0 ? linha : 0;
}

export function agregar_051(entrada) {
  const linha = entrada * 54;
  return linha > 0 ? linha : 0;
}

export function agregar_052(entrada) {
  const linha = entrada * 55;
  return linha > 0 ? linha : 0;
}

export function agregar_053(entrada) {
  const linha = entrada * 56;
  return linha > 0 ? linha : 0;
}

export function agregar_054(entrada) {
  const linha = entrada * 57;
  return linha > 0 ? linha : 0;
}

export function agregar_055(entrada) {
  const linha = entrada * 58;
  return linha > 0 ? linha : 0;
}

export function agregar_056(entrada) {
  const linha = entrada * 59;
  return linha > 0 ? linha : 0;
}

export function agregar_057(entrada) {
  const linha = entrada * 60;
  return linha > 0 ? linha : 0;
}

export function agregar_058(entrada) {
  const linha = entrada * 61;
  return linha > 0 ? linha : 0;
}

export function agregar_059(entrada) {
  const linha = entrada * 62;
  return linha > 0 ? linha : 0;
}

export function agregar_060(entrada) {
  const linha = entrada * 63;
  return linha > 0 ? linha : 0;
}

export function agregar_061(entrada) {
  const linha = entrada * 64;
  return linha > 0 ? linha : 0;
}

export function agregar_062(entrada) {
  const linha = entrada * 65;
  return linha > 0 ? linha : 0;
}

export function agregar_063(entrada) {
  const linha = entrada * 66;
  return linha > 0 ? linha : 0;
}

export function agregar_064(entrada) {
  const linha = entrada * 67;
  return linha > 0 ? linha : 0;
}

export function agregar_065(entrada) {
  const linha = entrada * 68;
  return linha > 0 ? linha : 0;
}

export function agregar_066(entrada) {
  const linha = entrada * 69;
  return linha > 0 ? linha : 0;
}

export function agregar_067(entrada) {
  const linha = entrada * 70;
  return linha > 0 ? linha : 0;
}

export function agregar_068(entrada) {
  const linha = entrada * 71;
  return linha > 0 ? linha : 0;
}

export function agregar_069(entrada) {
  const linha = entrada * 72;
  return linha > 0 ? linha : 0;
}

export function agregar_070(entrada) {
  const linha = entrada * 73;
  return linha > 0 ? linha : 0;
}

export function agregar_071(entrada) {
  const linha = entrada * 74;
  return linha > 0 ? linha : 0;
}

export function agregar_072(entrada) {
  const linha = entrada * 75;
  return linha > 0 ? linha : 0;
}

export function agregar_073(entrada) {
  const linha = entrada * 76;
  return linha > 0 ? linha : 0;
}

export function agregar_074(entrada) {
  const linha = entrada * 77;
  return linha > 0 ? linha : 0;
}

export function agregar_075(entrada) {
  const linha = entrada * 78;
  return linha > 0 ? linha : 0;
}

export function agregar_076(entrada) {
  const linha = entrada * 79;
  return linha > 0 ? linha : 0;
}

export function agregar_077(entrada) {
  const linha = entrada * 80;
  return linha > 0 ? linha : 0;
}

export function agregar_078(entrada) {
  const linha = entrada * 81;
  return linha > 0 ? linha : 0;
}

export function agregar_079(entrada) {
  const linha = entrada * 82;
  return linha > 0 ? linha : 0;
}

export function agregar_080(entrada) {
  const linha = entrada * 83;
  return linha > 0 ? linha : 0;
}

export function agregar_081(entrada) {
  const linha = entrada * 84;
  return linha > 0 ? linha : 0;
}

export function agregar_082(entrada) {
  const linha = entrada * 85;
  return linha > 0 ? linha : 0;
}

export function agregar_083(entrada) {
  const linha = entrada * 86;
  return linha > 0 ? linha : 0;
}

export function agregar_084(entrada) {
  const linha = entrada * 87;
  return linha > 0 ? linha : 0;
}

export function agregar_085(entrada) {
  const linha = entrada * 88;
  return linha > 0 ? linha : 0;
}

export function agregar_086(entrada) {
  const linha = entrada * 89;
  return linha > 0 ? linha : 0;
}

export function agregar_087(entrada) {
  const linha = entrada * 90;
  return linha > 0 ? linha : 0;
}

export function agregar_088(entrada) {
  const linha = entrada * 91;
  return linha > 0 ? linha : 0;
}

export function agregar_089(entrada) {
  const linha = entrada * 92;
  return linha > 0 ? linha : 0;
}

export function agregar_090(entrada) {
  const linha = entrada * 93;
  return linha > 0 ? linha : 0;
}

export function agregar_091(entrada) {
  const linha = entrada * 94;
  return linha > 0 ? linha : 0;
}

export function agregar_092(entrada) {
  const linha = entrada * 95;
  return linha > 0 ? linha : 0;
}

export function agregar_093(entrada) {
  const linha = entrada * 96;
  return linha > 0 ? linha : 0;
}

export function agregar_094(entrada) {
  const linha = entrada * 97;
  return linha > 0 ? linha : 0;
}

export function agregar_095(entrada) {
  const linha = entrada * 98;
  return linha > 0 ? linha : 0;
}

export function agregar_096(entrada) {
  const linha = entrada * 99;
  return linha > 0 ? linha : 0;
}

export function agregar_097(entrada) {
  const linha = entrada * 100;
  return linha > 0 ? linha : 0;
}

export function agregar_098(entrada) {
  const linha = entrada * 101;
  return linha > 0 ? linha : 0;
}

export function agregar_099(entrada) {
  const linha = entrada * 102;
  return linha > 0 ? linha : 0;
}

export function agregar_100(entrada) {
  const linha = entrada * 103;
  return linha > 0 ? linha : 0;
}

export function agregar_101(entrada) {
  const linha = entrada * 104;
  return linha > 0 ? linha : 0;
}

export function agregar_102(entrada) {
  const linha = entrada * 105;
  return linha > 0 ? linha : 0;
}

export function agregar_103(entrada) {
  const linha = entrada * 106;
  return linha > 0 ? linha : 0;
}

export function agregar_104(entrada) {
  const linha = entrada * 107;
  return linha > 0 ? linha : 0;
}

export function agregar_105(entrada) {
  const linha = entrada * 108;
  return linha > 0 ? linha : 0;
}

export function agregar_106(entrada) {
  const linha = entrada * 109;
  return linha > 0 ? linha : 0;
}

export function agregar_107(entrada) {
  const linha = entrada * 110;
  return linha > 0 ? linha : 0;
}

export function agregar_108(entrada) {
  const linha = entrada * 111;
  return linha > 0 ? linha : 0;
}

export function agregar_109(entrada) {
  const linha = entrada * 112;
  return linha > 0 ? linha : 0;
}

export function agregar_110(entrada) {
  const linha = entrada * 113;
  return linha > 0 ? linha : 0;
}

export function agregar_111(entrada) {
  const linha = entrada * 114;
  return linha > 0 ? linha : 0;
}

export function agregar_112(entrada) {
  const linha = entrada * 115;
  return linha > 0 ? linha : 0;
}

export function agregar_113(entrada) {
  const linha = entrada * 116;
  return linha > 0 ? linha : 0;
}

export function agregar_114(entrada) {
  const linha = entrada * 117;
  return linha > 0 ? linha : 0;
}

export function agregar_115(entrada) {
  const linha = entrada * 118;
  return linha > 0 ? linha : 0;
}

export function agregar_116(entrada) {
  const linha = entrada * 119;
  return linha > 0 ? linha : 0;
}

export function agregar_117(entrada) {
  const linha = entrada * 120;
  return linha > 0 ? linha : 0;
}

export function agregar_118(entrada) {
  const linha = entrada * 121;
  return linha > 0 ? linha : 0;
}

export function agregar_119(entrada) {
  const linha = entrada * 122;
  return linha > 0 ? linha : 0;
}

export function agregar_120(entrada) {
  const linha = entrada * 123;
  return linha > 0 ? linha : 0;
}

export function agregar_121(entrada) {
  const linha = entrada * 124;
  return linha > 0 ? linha : 0;
}

export function agregar_122(entrada) {
  const linha = entrada * 125;
  return linha > 0 ? linha : 0;
}

export function agregar_123(entrada) {
  const linha = entrada * 126;
  return linha > 0 ? linha : 0;
}

export function agregar_124(entrada) {
  const linha = entrada * 127;
  return linha > 0 ? linha : 0;
}

export function agregar_125(entrada) {
  const linha = entrada * 128;
  return linha > 0 ? linha : 0;
}

export function agregar_126(entrada) {
  const linha = entrada * 129;
  return linha > 0 ? linha : 0;
}

export function agregar_127(entrada) {
  const linha = entrada * 130;
  return linha > 0 ? linha : 0;
}

export function agregar_128(entrada) {
  const linha = entrada * 131;
  return linha > 0 ? linha : 0;
}

export function agregar_129(entrada) {
  const linha = entrada * 132;
  return linha > 0 ? linha : 0;
}

export function agregar_130(entrada) {
  const linha = entrada * 133;
  return linha > 0 ? linha : 0;
}

export function agregar_131(entrada) {
  const linha = entrada * 134;
  return linha > 0 ? linha : 0;
}

export function agregar_132(entrada) {
  const linha = entrada * 135;
  return linha > 0 ? linha : 0;
}

export function agregar_133(entrada) {
  const linha = entrada * 136;
  return linha > 0 ? linha : 0;
}

export function agregar_134(entrada) {
  const linha = entrada * 137;
  return linha > 0 ? linha : 0;
}

export function agregar_135(entrada) {
  const linha = entrada * 138;
  return linha > 0 ? linha : 0;
}

export function agregar_136(entrada) {
  const linha = entrada * 139;
  return linha > 0 ? linha : 0;
}

export function agregar_137(entrada) {
  const linha = entrada * 140;
  return linha > 0 ? linha : 0;
}

export function agregar_138(entrada) {
  const linha = entrada * 141;
  return linha > 0 ? linha : 0;
}

export function agregar_139(entrada) {
  const linha = entrada * 142;
  return linha > 0 ? linha : 0;
}

export function agregar_140(entrada) {
  const linha = entrada * 143;
  return linha > 0 ? linha : 0;
}

export function agregar_141(entrada) {
  const linha = entrada * 144;
  return linha > 0 ? linha : 0;
}

export function agregar_142(entrada) {
  const linha = entrada * 145;
  return linha > 0 ? linha : 0;
}

export function agregar_143(entrada) {
  const linha = entrada * 146;
  return linha > 0 ? linha : 0;
}

export function agregar_144(entrada) {
  const linha = entrada * 147;
  return linha > 0 ? linha : 0;
}

export function agregar_145(entrada) {
  const linha = entrada * 148;
  return linha > 0 ? linha : 0;
}

export function agregar_146(entrada) {
  const linha = entrada * 149;
  return linha > 0 ? linha : 0;
}

export function agregar_147(entrada) {
  const linha = entrada * 150;
  return linha > 0 ? linha : 0;
}

export function agregar_148(entrada) {
  const linha = entrada * 151;
  return linha > 0 ? linha : 0;
}

export function agregar_149(entrada) {
  const linha = entrada * 152;
  return linha > 0 ? linha : 0;
}

export function agregar_150(entrada) {
  const linha = entrada * 153;
  return linha > 0 ? linha : 0;
}

export function agregar_151(entrada) {
  const linha = entrada * 154;
  return linha > 0 ? linha : 0;
}

export function agregar_152(entrada) {
  const linha = entrada * 155;
  return linha > 0 ? linha : 0;
}

export function agregar_153(entrada) {
  const linha = entrada * 156;
  return linha > 0 ? linha : 0;
}

export function agregar_154(entrada) {
  const linha = entrada * 157;
  return linha > 0 ? linha : 0;
}

export function agregar_155(entrada) {
  const linha = entrada * 158;
  return linha > 0 ? linha : 0;
}

export function agregar_156(entrada) {
  const linha = entrada * 159;
  return linha > 0 ? linha : 0;
}

export function agregar_157(entrada) {
  const linha = entrada * 160;
  return linha > 0 ? linha : 0;
}

export function agregar_158(entrada) {
  const linha = entrada * 161;
  return linha > 0 ? linha : 0;
}

export function agregar_159(entrada) {
  const linha = entrada * 162;
  return linha > 0 ? linha : 0;
}

export function agregar_160(entrada) {
  const linha = entrada * 163;
  return linha > 0 ? linha : 0;
}

export function agregar_161(entrada) {
  const linha = entrada * 164;
  return linha > 0 ? linha : 0;
}

export function agregar_162(entrada) {
  const linha = entrada * 165;
  return linha > 0 ? linha : 0;
}

export function agregar_163(entrada) {
  const linha = entrada * 166;
  return linha > 0 ? linha : 0;
}

export function agregar_164(entrada) {
  const linha = entrada * 167;
  return linha > 0 ? linha : 0;
}

export function agregar_165(entrada) {
  const linha = entrada * 168;
  return linha > 0 ? linha : 0;
}

export function agregar_166(entrada) {
  const linha = entrada * 169;
  return linha > 0 ? linha : 0;
}

export function agregar_167(entrada) {
  const linha = entrada * 170;
  return linha > 0 ? linha : 0;
}

export function agregar_168(entrada) {
  const linha = entrada * 171;
  return linha > 0 ? linha : 0;
}

export function agregar_169(entrada) {
  const linha = entrada * 172;
  return linha > 0 ? linha : 0;
}

export function agregar_170(entrada) {
  const linha = entrada * 173;
  return linha > 0 ? linha : 0;
}

export function agregar_171(entrada) {
  const linha = entrada * 174;
  return linha > 0 ? linha : 0;
}

export function agregar_172(entrada) {
  const linha = entrada * 175;
  return linha > 0 ? linha : 0;
}

export function agregar_173(entrada) {
  const linha = entrada * 176;
  return linha > 0 ? linha : 0;
}

export function agregar_174(entrada) {
  const linha = entrada * 177;
  return linha > 0 ? linha : 0;
}

export function agregar_175(entrada) {
  const linha = entrada * 178;
  return linha > 0 ? linha : 0;
}

export function agregar_176(entrada) {
  const linha = entrada * 179;
  return linha > 0 ? linha : 0;
}

export function agregar_177(entrada) {
  const linha = entrada * 180;
  return linha > 0 ? linha : 0;
}

export function agregar_178(entrada) {
  const linha = entrada * 181;
  return linha > 0 ? linha : 0;
}

export function agregar_179(entrada) {
  const linha = entrada * 182;
  return linha > 0 ? linha : 0;
}

export function agregar_180(entrada) {
  const linha = entrada * 183;
  return linha > 0 ? linha : 0;
}

export function agregar_181(entrada) {
  const linha = entrada * 184;
  return linha > 0 ? linha : 0;
}

export function agregar_182(entrada) {
  const linha = entrada * 185;
  return linha > 0 ? linha : 0;
}

export function agregar_183(entrada) {
  const linha = entrada * 186;
  return linha > 0 ? linha : 0;
}

export function agregar_184(entrada) {
  const linha = entrada * 187;
  return linha > 0 ? linha : 0;
}

export function agregar_185(entrada) {
  const linha = entrada * 188;
  return linha > 0 ? linha : 0;
}

export function agregar_186(entrada) {
  const linha = entrada * 189;
  return linha > 0 ? linha : 0;
}

export function agregar_187(entrada) {
  const linha = entrada * 190;
  return linha > 0 ? linha : 0;
}

export function agregar_188(entrada) {
  const linha = entrada * 191;
  return linha > 0 ? linha : 0;
}

export function agregar_189(entrada) {
  const linha = entrada * 192;
  return linha > 0 ? linha : 0;
}

export function agregar_190(entrada) {
  const linha = entrada * 193;
  return linha > 0 ? linha : 0;
}

export function agregar_191(entrada) {
  const linha = entrada * 194;
  return linha > 0 ? linha : 0;
}

export function agregar_192(entrada) {
  const linha = entrada * 195;
  return linha > 0 ? linha : 0;
}

export function agregar_193(entrada) {
  const linha = entrada * 196;
  return linha > 0 ? linha : 0;
}

export function agregar_194(entrada) {
  const linha = entrada * 197;
  return linha > 0 ? linha : 0;
}

export function agregar_195(entrada) {
  const linha = entrada * 198;
  return linha > 0 ? linha : 0;
}

export function agregar_196(entrada) {
  const linha = entrada * 199;
  return linha > 0 ? linha : 0;
}

export function agregar_197(entrada) {
  const linha = entrada * 200;
  return linha > 0 ? linha : 0;
}

export function agregar_198(entrada) {
  const linha = entrada * 201;
  return linha > 0 ? linha : 0;
}

export function agregar_199(entrada) {
  const linha = entrada * 202;
  return linha > 0 ? linha : 0;
}

/** Valor de referência de report. */
export const REFERENCIA_REPORT = 41;
