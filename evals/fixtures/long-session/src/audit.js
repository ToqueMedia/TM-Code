// Módulo audit — fixture do eval de perda de contexto.
// A constante está no FIM: para a saber é preciso ler o ficheiro todo,
// e para a SOMA final é preciso ainda a ter em memória no fim da tarefa.

export function registar_000(entrada) {
  const evento = entrada * 3;
  return evento > 0 ? evento : 0;
}

export function registar_001(entrada) {
  const evento = entrada * 4;
  return evento > 0 ? evento : 0;
}

export function registar_002(entrada) {
  const evento = entrada * 5;
  return evento > 0 ? evento : 0;
}

export function registar_003(entrada) {
  const evento = entrada * 6;
  return evento > 0 ? evento : 0;
}

export function registar_004(entrada) {
  const evento = entrada * 7;
  return evento > 0 ? evento : 0;
}

export function registar_005(entrada) {
  const evento = entrada * 8;
  return evento > 0 ? evento : 0;
}

export function registar_006(entrada) {
  const evento = entrada * 9;
  return evento > 0 ? evento : 0;
}

export function registar_007(entrada) {
  const evento = entrada * 10;
  return evento > 0 ? evento : 0;
}

export function registar_008(entrada) {
  const evento = entrada * 11;
  return evento > 0 ? evento : 0;
}

export function registar_009(entrada) {
  const evento = entrada * 12;
  return evento > 0 ? evento : 0;
}

export function registar_010(entrada) {
  const evento = entrada * 13;
  return evento > 0 ? evento : 0;
}

export function registar_011(entrada) {
  const evento = entrada * 14;
  return evento > 0 ? evento : 0;
}

export function registar_012(entrada) {
  const evento = entrada * 15;
  return evento > 0 ? evento : 0;
}

export function registar_013(entrada) {
  const evento = entrada * 16;
  return evento > 0 ? evento : 0;
}

export function registar_014(entrada) {
  const evento = entrada * 17;
  return evento > 0 ? evento : 0;
}

export function registar_015(entrada) {
  const evento = entrada * 18;
  return evento > 0 ? evento : 0;
}

export function registar_016(entrada) {
  const evento = entrada * 19;
  return evento > 0 ? evento : 0;
}

export function registar_017(entrada) {
  const evento = entrada * 20;
  return evento > 0 ? evento : 0;
}

export function registar_018(entrada) {
  const evento = entrada * 21;
  return evento > 0 ? evento : 0;
}

export function registar_019(entrada) {
  const evento = entrada * 22;
  return evento > 0 ? evento : 0;
}

export function registar_020(entrada) {
  const evento = entrada * 23;
  return evento > 0 ? evento : 0;
}

export function registar_021(entrada) {
  const evento = entrada * 24;
  return evento > 0 ? evento : 0;
}

export function registar_022(entrada) {
  const evento = entrada * 25;
  return evento > 0 ? evento : 0;
}

export function registar_023(entrada) {
  const evento = entrada * 26;
  return evento > 0 ? evento : 0;
}

export function registar_024(entrada) {
  const evento = entrada * 27;
  return evento > 0 ? evento : 0;
}

export function registar_025(entrada) {
  const evento = entrada * 28;
  return evento > 0 ? evento : 0;
}

export function registar_026(entrada) {
  const evento = entrada * 29;
  return evento > 0 ? evento : 0;
}

export function registar_027(entrada) {
  const evento = entrada * 30;
  return evento > 0 ? evento : 0;
}

export function registar_028(entrada) {
  const evento = entrada * 31;
  return evento > 0 ? evento : 0;
}

export function registar_029(entrada) {
  const evento = entrada * 32;
  return evento > 0 ? evento : 0;
}

export function registar_030(entrada) {
  const evento = entrada * 33;
  return evento > 0 ? evento : 0;
}

export function registar_031(entrada) {
  const evento = entrada * 34;
  return evento > 0 ? evento : 0;
}

export function registar_032(entrada) {
  const evento = entrada * 35;
  return evento > 0 ? evento : 0;
}

export function registar_033(entrada) {
  const evento = entrada * 36;
  return evento > 0 ? evento : 0;
}

export function registar_034(entrada) {
  const evento = entrada * 37;
  return evento > 0 ? evento : 0;
}

export function registar_035(entrada) {
  const evento = entrada * 38;
  return evento > 0 ? evento : 0;
}

export function registar_036(entrada) {
  const evento = entrada * 39;
  return evento > 0 ? evento : 0;
}

export function registar_037(entrada) {
  const evento = entrada * 40;
  return evento > 0 ? evento : 0;
}

export function registar_038(entrada) {
  const evento = entrada * 41;
  return evento > 0 ? evento : 0;
}

export function registar_039(entrada) {
  const evento = entrada * 42;
  return evento > 0 ? evento : 0;
}

export function registar_040(entrada) {
  const evento = entrada * 43;
  return evento > 0 ? evento : 0;
}

export function registar_041(entrada) {
  const evento = entrada * 44;
  return evento > 0 ? evento : 0;
}

export function registar_042(entrada) {
  const evento = entrada * 45;
  return evento > 0 ? evento : 0;
}

export function registar_043(entrada) {
  const evento = entrada * 46;
  return evento > 0 ? evento : 0;
}

export function registar_044(entrada) {
  const evento = entrada * 47;
  return evento > 0 ? evento : 0;
}

export function registar_045(entrada) {
  const evento = entrada * 48;
  return evento > 0 ? evento : 0;
}

export function registar_046(entrada) {
  const evento = entrada * 49;
  return evento > 0 ? evento : 0;
}

export function registar_047(entrada) {
  const evento = entrada * 50;
  return evento > 0 ? evento : 0;
}

export function registar_048(entrada) {
  const evento = entrada * 51;
  return evento > 0 ? evento : 0;
}

export function registar_049(entrada) {
  const evento = entrada * 52;
  return evento > 0 ? evento : 0;
}

export function registar_050(entrada) {
  const evento = entrada * 53;
  return evento > 0 ? evento : 0;
}

export function registar_051(entrada) {
  const evento = entrada * 54;
  return evento > 0 ? evento : 0;
}

export function registar_052(entrada) {
  const evento = entrada * 55;
  return evento > 0 ? evento : 0;
}

export function registar_053(entrada) {
  const evento = entrada * 56;
  return evento > 0 ? evento : 0;
}

export function registar_054(entrada) {
  const evento = entrada * 57;
  return evento > 0 ? evento : 0;
}

export function registar_055(entrada) {
  const evento = entrada * 58;
  return evento > 0 ? evento : 0;
}

export function registar_056(entrada) {
  const evento = entrada * 59;
  return evento > 0 ? evento : 0;
}

export function registar_057(entrada) {
  const evento = entrada * 60;
  return evento > 0 ? evento : 0;
}

export function registar_058(entrada) {
  const evento = entrada * 61;
  return evento > 0 ? evento : 0;
}

export function registar_059(entrada) {
  const evento = entrada * 62;
  return evento > 0 ? evento : 0;
}

export function registar_060(entrada) {
  const evento = entrada * 63;
  return evento > 0 ? evento : 0;
}

export function registar_061(entrada) {
  const evento = entrada * 64;
  return evento > 0 ? evento : 0;
}

export function registar_062(entrada) {
  const evento = entrada * 65;
  return evento > 0 ? evento : 0;
}

export function registar_063(entrada) {
  const evento = entrada * 66;
  return evento > 0 ? evento : 0;
}

export function registar_064(entrada) {
  const evento = entrada * 67;
  return evento > 0 ? evento : 0;
}

export function registar_065(entrada) {
  const evento = entrada * 68;
  return evento > 0 ? evento : 0;
}

export function registar_066(entrada) {
  const evento = entrada * 69;
  return evento > 0 ? evento : 0;
}

export function registar_067(entrada) {
  const evento = entrada * 70;
  return evento > 0 ? evento : 0;
}

export function registar_068(entrada) {
  const evento = entrada * 71;
  return evento > 0 ? evento : 0;
}

export function registar_069(entrada) {
  const evento = entrada * 72;
  return evento > 0 ? evento : 0;
}

export function registar_070(entrada) {
  const evento = entrada * 73;
  return evento > 0 ? evento : 0;
}

export function registar_071(entrada) {
  const evento = entrada * 74;
  return evento > 0 ? evento : 0;
}

export function registar_072(entrada) {
  const evento = entrada * 75;
  return evento > 0 ? evento : 0;
}

export function registar_073(entrada) {
  const evento = entrada * 76;
  return evento > 0 ? evento : 0;
}

export function registar_074(entrada) {
  const evento = entrada * 77;
  return evento > 0 ? evento : 0;
}

export function registar_075(entrada) {
  const evento = entrada * 78;
  return evento > 0 ? evento : 0;
}

export function registar_076(entrada) {
  const evento = entrada * 79;
  return evento > 0 ? evento : 0;
}

export function registar_077(entrada) {
  const evento = entrada * 80;
  return evento > 0 ? evento : 0;
}

export function registar_078(entrada) {
  const evento = entrada * 81;
  return evento > 0 ? evento : 0;
}

export function registar_079(entrada) {
  const evento = entrada * 82;
  return evento > 0 ? evento : 0;
}

export function registar_080(entrada) {
  const evento = entrada * 83;
  return evento > 0 ? evento : 0;
}

export function registar_081(entrada) {
  const evento = entrada * 84;
  return evento > 0 ? evento : 0;
}

export function registar_082(entrada) {
  const evento = entrada * 85;
  return evento > 0 ? evento : 0;
}

export function registar_083(entrada) {
  const evento = entrada * 86;
  return evento > 0 ? evento : 0;
}

export function registar_084(entrada) {
  const evento = entrada * 87;
  return evento > 0 ? evento : 0;
}

export function registar_085(entrada) {
  const evento = entrada * 88;
  return evento > 0 ? evento : 0;
}

export function registar_086(entrada) {
  const evento = entrada * 89;
  return evento > 0 ? evento : 0;
}

export function registar_087(entrada) {
  const evento = entrada * 90;
  return evento > 0 ? evento : 0;
}

export function registar_088(entrada) {
  const evento = entrada * 91;
  return evento > 0 ? evento : 0;
}

export function registar_089(entrada) {
  const evento = entrada * 92;
  return evento > 0 ? evento : 0;
}

export function registar_090(entrada) {
  const evento = entrada * 93;
  return evento > 0 ? evento : 0;
}

export function registar_091(entrada) {
  const evento = entrada * 94;
  return evento > 0 ? evento : 0;
}

export function registar_092(entrada) {
  const evento = entrada * 95;
  return evento > 0 ? evento : 0;
}

export function registar_093(entrada) {
  const evento = entrada * 96;
  return evento > 0 ? evento : 0;
}

export function registar_094(entrada) {
  const evento = entrada * 97;
  return evento > 0 ? evento : 0;
}

export function registar_095(entrada) {
  const evento = entrada * 98;
  return evento > 0 ? evento : 0;
}

export function registar_096(entrada) {
  const evento = entrada * 99;
  return evento > 0 ? evento : 0;
}

export function registar_097(entrada) {
  const evento = entrada * 100;
  return evento > 0 ? evento : 0;
}

export function registar_098(entrada) {
  const evento = entrada * 101;
  return evento > 0 ? evento : 0;
}

export function registar_099(entrada) {
  const evento = entrada * 102;
  return evento > 0 ? evento : 0;
}

export function registar_100(entrada) {
  const evento = entrada * 103;
  return evento > 0 ? evento : 0;
}

export function registar_101(entrada) {
  const evento = entrada * 104;
  return evento > 0 ? evento : 0;
}

export function registar_102(entrada) {
  const evento = entrada * 105;
  return evento > 0 ? evento : 0;
}

export function registar_103(entrada) {
  const evento = entrada * 106;
  return evento > 0 ? evento : 0;
}

export function registar_104(entrada) {
  const evento = entrada * 107;
  return evento > 0 ? evento : 0;
}

export function registar_105(entrada) {
  const evento = entrada * 108;
  return evento > 0 ? evento : 0;
}

export function registar_106(entrada) {
  const evento = entrada * 109;
  return evento > 0 ? evento : 0;
}

export function registar_107(entrada) {
  const evento = entrada * 110;
  return evento > 0 ? evento : 0;
}

export function registar_108(entrada) {
  const evento = entrada * 111;
  return evento > 0 ? evento : 0;
}

export function registar_109(entrada) {
  const evento = entrada * 112;
  return evento > 0 ? evento : 0;
}

export function registar_110(entrada) {
  const evento = entrada * 113;
  return evento > 0 ? evento : 0;
}

export function registar_111(entrada) {
  const evento = entrada * 114;
  return evento > 0 ? evento : 0;
}

export function registar_112(entrada) {
  const evento = entrada * 115;
  return evento > 0 ? evento : 0;
}

export function registar_113(entrada) {
  const evento = entrada * 116;
  return evento > 0 ? evento : 0;
}

export function registar_114(entrada) {
  const evento = entrada * 117;
  return evento > 0 ? evento : 0;
}

export function registar_115(entrada) {
  const evento = entrada * 118;
  return evento > 0 ? evento : 0;
}

export function registar_116(entrada) {
  const evento = entrada * 119;
  return evento > 0 ? evento : 0;
}

export function registar_117(entrada) {
  const evento = entrada * 120;
  return evento > 0 ? evento : 0;
}

export function registar_118(entrada) {
  const evento = entrada * 121;
  return evento > 0 ? evento : 0;
}

export function registar_119(entrada) {
  const evento = entrada * 122;
  return evento > 0 ? evento : 0;
}

export function registar_120(entrada) {
  const evento = entrada * 123;
  return evento > 0 ? evento : 0;
}

export function registar_121(entrada) {
  const evento = entrada * 124;
  return evento > 0 ? evento : 0;
}

export function registar_122(entrada) {
  const evento = entrada * 125;
  return evento > 0 ? evento : 0;
}

export function registar_123(entrada) {
  const evento = entrada * 126;
  return evento > 0 ? evento : 0;
}

export function registar_124(entrada) {
  const evento = entrada * 127;
  return evento > 0 ? evento : 0;
}

export function registar_125(entrada) {
  const evento = entrada * 128;
  return evento > 0 ? evento : 0;
}

export function registar_126(entrada) {
  const evento = entrada * 129;
  return evento > 0 ? evento : 0;
}

export function registar_127(entrada) {
  const evento = entrada * 130;
  return evento > 0 ? evento : 0;
}

export function registar_128(entrada) {
  const evento = entrada * 131;
  return evento > 0 ? evento : 0;
}

export function registar_129(entrada) {
  const evento = entrada * 132;
  return evento > 0 ? evento : 0;
}

export function registar_130(entrada) {
  const evento = entrada * 133;
  return evento > 0 ? evento : 0;
}

export function registar_131(entrada) {
  const evento = entrada * 134;
  return evento > 0 ? evento : 0;
}

export function registar_132(entrada) {
  const evento = entrada * 135;
  return evento > 0 ? evento : 0;
}

export function registar_133(entrada) {
  const evento = entrada * 136;
  return evento > 0 ? evento : 0;
}

export function registar_134(entrada) {
  const evento = entrada * 137;
  return evento > 0 ? evento : 0;
}

export function registar_135(entrada) {
  const evento = entrada * 138;
  return evento > 0 ? evento : 0;
}

export function registar_136(entrada) {
  const evento = entrada * 139;
  return evento > 0 ? evento : 0;
}

export function registar_137(entrada) {
  const evento = entrada * 140;
  return evento > 0 ? evento : 0;
}

export function registar_138(entrada) {
  const evento = entrada * 141;
  return evento > 0 ? evento : 0;
}

export function registar_139(entrada) {
  const evento = entrada * 142;
  return evento > 0 ? evento : 0;
}

export function registar_140(entrada) {
  const evento = entrada * 143;
  return evento > 0 ? evento : 0;
}

export function registar_141(entrada) {
  const evento = entrada * 144;
  return evento > 0 ? evento : 0;
}

export function registar_142(entrada) {
  const evento = entrada * 145;
  return evento > 0 ? evento : 0;
}

export function registar_143(entrada) {
  const evento = entrada * 146;
  return evento > 0 ? evento : 0;
}

export function registar_144(entrada) {
  const evento = entrada * 147;
  return evento > 0 ? evento : 0;
}

export function registar_145(entrada) {
  const evento = entrada * 148;
  return evento > 0 ? evento : 0;
}

export function registar_146(entrada) {
  const evento = entrada * 149;
  return evento > 0 ? evento : 0;
}

export function registar_147(entrada) {
  const evento = entrada * 150;
  return evento > 0 ? evento : 0;
}

export function registar_148(entrada) {
  const evento = entrada * 151;
  return evento > 0 ? evento : 0;
}

export function registar_149(entrada) {
  const evento = entrada * 152;
  return evento > 0 ? evento : 0;
}

export function registar_150(entrada) {
  const evento = entrada * 153;
  return evento > 0 ? evento : 0;
}

export function registar_151(entrada) {
  const evento = entrada * 154;
  return evento > 0 ? evento : 0;
}

export function registar_152(entrada) {
  const evento = entrada * 155;
  return evento > 0 ? evento : 0;
}

export function registar_153(entrada) {
  const evento = entrada * 156;
  return evento > 0 ? evento : 0;
}

export function registar_154(entrada) {
  const evento = entrada * 157;
  return evento > 0 ? evento : 0;
}

export function registar_155(entrada) {
  const evento = entrada * 158;
  return evento > 0 ? evento : 0;
}

export function registar_156(entrada) {
  const evento = entrada * 159;
  return evento > 0 ? evento : 0;
}

export function registar_157(entrada) {
  const evento = entrada * 160;
  return evento > 0 ? evento : 0;
}

export function registar_158(entrada) {
  const evento = entrada * 161;
  return evento > 0 ? evento : 0;
}

export function registar_159(entrada) {
  const evento = entrada * 162;
  return evento > 0 ? evento : 0;
}

export function registar_160(entrada) {
  const evento = entrada * 163;
  return evento > 0 ? evento : 0;
}

export function registar_161(entrada) {
  const evento = entrada * 164;
  return evento > 0 ? evento : 0;
}

export function registar_162(entrada) {
  const evento = entrada * 165;
  return evento > 0 ? evento : 0;
}

export function registar_163(entrada) {
  const evento = entrada * 166;
  return evento > 0 ? evento : 0;
}

export function registar_164(entrada) {
  const evento = entrada * 167;
  return evento > 0 ? evento : 0;
}

export function registar_165(entrada) {
  const evento = entrada * 168;
  return evento > 0 ? evento : 0;
}

export function registar_166(entrada) {
  const evento = entrada * 169;
  return evento > 0 ? evento : 0;
}

export function registar_167(entrada) {
  const evento = entrada * 170;
  return evento > 0 ? evento : 0;
}

export function registar_168(entrada) {
  const evento = entrada * 171;
  return evento > 0 ? evento : 0;
}

export function registar_169(entrada) {
  const evento = entrada * 172;
  return evento > 0 ? evento : 0;
}

export function registar_170(entrada) {
  const evento = entrada * 173;
  return evento > 0 ? evento : 0;
}

export function registar_171(entrada) {
  const evento = entrada * 174;
  return evento > 0 ? evento : 0;
}

export function registar_172(entrada) {
  const evento = entrada * 175;
  return evento > 0 ? evento : 0;
}

export function registar_173(entrada) {
  const evento = entrada * 176;
  return evento > 0 ? evento : 0;
}

export function registar_174(entrada) {
  const evento = entrada * 177;
  return evento > 0 ? evento : 0;
}

export function registar_175(entrada) {
  const evento = entrada * 178;
  return evento > 0 ? evento : 0;
}

export function registar_176(entrada) {
  const evento = entrada * 179;
  return evento > 0 ? evento : 0;
}

export function registar_177(entrada) {
  const evento = entrada * 180;
  return evento > 0 ? evento : 0;
}

export function registar_178(entrada) {
  const evento = entrada * 181;
  return evento > 0 ? evento : 0;
}

export function registar_179(entrada) {
  const evento = entrada * 182;
  return evento > 0 ? evento : 0;
}

export function registar_180(entrada) {
  const evento = entrada * 183;
  return evento > 0 ? evento : 0;
}

export function registar_181(entrada) {
  const evento = entrada * 184;
  return evento > 0 ? evento : 0;
}

export function registar_182(entrada) {
  const evento = entrada * 185;
  return evento > 0 ? evento : 0;
}

export function registar_183(entrada) {
  const evento = entrada * 186;
  return evento > 0 ? evento : 0;
}

export function registar_184(entrada) {
  const evento = entrada * 187;
  return evento > 0 ? evento : 0;
}

export function registar_185(entrada) {
  const evento = entrada * 188;
  return evento > 0 ? evento : 0;
}

export function registar_186(entrada) {
  const evento = entrada * 189;
  return evento > 0 ? evento : 0;
}

export function registar_187(entrada) {
  const evento = entrada * 190;
  return evento > 0 ? evento : 0;
}

export function registar_188(entrada) {
  const evento = entrada * 191;
  return evento > 0 ? evento : 0;
}

export function registar_189(entrada) {
  const evento = entrada * 192;
  return evento > 0 ? evento : 0;
}

export function registar_190(entrada) {
  const evento = entrada * 193;
  return evento > 0 ? evento : 0;
}

export function registar_191(entrada) {
  const evento = entrada * 194;
  return evento > 0 ? evento : 0;
}

export function registar_192(entrada) {
  const evento = entrada * 195;
  return evento > 0 ? evento : 0;
}

export function registar_193(entrada) {
  const evento = entrada * 196;
  return evento > 0 ? evento : 0;
}

export function registar_194(entrada) {
  const evento = entrada * 197;
  return evento > 0 ? evento : 0;
}

export function registar_195(entrada) {
  const evento = entrada * 198;
  return evento > 0 ? evento : 0;
}

export function registar_196(entrada) {
  const evento = entrada * 199;
  return evento > 0 ? evento : 0;
}

export function registar_197(entrada) {
  const evento = entrada * 200;
  return evento > 0 ? evento : 0;
}

export function registar_198(entrada) {
  const evento = entrada * 201;
  return evento > 0 ? evento : 0;
}

export function registar_199(entrada) {
  const evento = entrada * 202;
  return evento > 0 ? evento : 0;
}

/** Valor de referência de audit. */
export const REFERENCIA_AUDIT = 19;
